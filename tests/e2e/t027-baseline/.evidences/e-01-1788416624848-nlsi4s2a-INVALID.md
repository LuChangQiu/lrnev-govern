# sha-b 无效证据说明

**文件**: `e-01-1788416624848-nlsi4s2a.{json,jsonl}`

**标注**: ❌ **无效观测** - frontmatter bug 导致 -32602 错误

## 无效原因

harness buildWorkspace 的 frontmatter 日期格式错误：
- `created: 2026-09-03`（无引号）→ YAML 解析为 Date 对象
- sha-b 的 outputSchema 校验报错：expected string, received Date

## 影响

- `project_status` 和 `scene_list` 双双返回 MCP error -32602
- AI 无法获取 scene 信息，spec_create 未带 scene 参数
- spec 建到 `00-default` 而非期望的 `01-user-management`

## 根因

harness 格式与真实项目不一致：
- 真实项目：`created: '2026-08-26'`（带引号 → string）
- harness（bug 版）：`created: 2026-09-03`（无引号 → Date）

## 修复

提交 `f331a3f` 修复 frontmatter 格式（所有 created 字段加引号）

## 有效证据

修复后第 3 次运行：`e-01-1788419034219-05n45akm.{json,jsonl}` ✅
- 无 -32602 错误
- AI 正常调用 project_status
- spec 正确落位 01-user-management
