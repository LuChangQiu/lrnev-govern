# E-05 无效证据说明（attempt 1，pre-driveClient 加固）

**文件**: `e-05-1788483831358-kdmd2fz4.{json,-rounds.jsonl,-round1.jsonl,-round2.jsonl,-session.jsonl}`

**标注**: ❌ **无效观测** - prompt 投递缺陷（round2 会话收到 `"."` 伪用户输入 + 上一会话残留上下文）

## 无效原因

harness driveClient 旧实现用 `-p <JSON 字符串>` 经 Windows cmd（shell: true）传参：
- 含内嵌双引号的轮次话术投递不可靠，round2 会话模型实际只收到单个 `"."`
  （模型 thinking 原文："The user just sent a single period '.'... previous message was 第3轮：..."），
  并以问候语收场（0 工具调用）——**非模型对 E-05 场景的真实行为**。

## 修复

提交 harness-mvp.mjs driveClient 加固（stdin 直传 prompt + `--no-session-persistence`）后：
- round2 话术稳定投递，模型正常行动（见 `e-05-1788484671460-ptxrb7vb` 有效样本：round2 AI 调用 spec_create）

## 有效证据

加固后：`e-05-1788484671460-ptxrb7vb.{json,-rounds.jsonl,-round1.jsonl,-round2.jsonl,-session.jsonl}`（FAIL：AI 建到 00-default，scene 参数不匹配——真实 AI 行为差异，见该样本 sidecar）
