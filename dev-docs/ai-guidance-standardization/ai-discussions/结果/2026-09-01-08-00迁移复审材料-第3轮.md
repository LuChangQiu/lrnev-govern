# 08-00 运行时迁移复审材料（第 3 轮）

**提交时间**: 2026-09-01
**复审轮次**: 第 3 轮（修复 F-05/F-08/F-09 + 授权偏离落档）
**对照基准**: 08-00 requirements.md F-01~F-10 + 用户授权（2026-09-01）

---

## 一、完整 diff 汇总（8 文件修改）

### 1. 新增文件

#### `src/core/guidance-semantics.ts` (99 行)
**目的**: F-01 共享语义常量，确保跨文件 guidance 语义一致

**核心常量**:
- `USER_DECISION_PRIORITY_CLAUSE`: 用户决定优先条款
- `SPEC_CREATION_SUCCESS_FOLLOWUP`: 创建成功后不得撤销
- `SPEC_REUSE_DECISION_RUBRIC`: 三条判断标尺（整体推翻/独立特性/上下文冷却）
- `GOAL_ASSESSOR_OVERRIDE_CLAUSE`: suggested_next_step 是建议非必须

**语义边界**: 明确区分 RECOMMENDATION（可 override）与 EXECUTION_CONSTRAINT（真实约束）

---

### 2. 修改文件

#### `docs/AI-ADAPTATION.md` (F-02)
**位置**: L142
**原文**: `不新开 spec`
**改后**: `通常无需新开 spec`
**理由**: 消除黑名单强制措辞

**位置**: L150-155（新增）
**内容**: 三条判断标尺 + 用户决定优先条款
- 整体推翻 → 开新版（version+1）
- 独立特性 → 开新 Spec
- 上下文冷却 → 先读摘要
- 用户决定优先于任何建议

---

#### `src/mcp/guidance.ts` (F-03/F-04)

**F-03: WORKFLOW_OVERVIEW** (L1-11)
**改动**: 引入 `USER_DECISION_PRIORITY_CLAUSE` 作为第 6 个元素
**理由**: 全局工作流加入用户决定优先声明

**F-04: spec_create 描述** (L28)
**原文**: `是才开 spec`
**改后**: `通常适合为这类特性开 spec`
**理由**: 消除黑名单措辞，改为建议语气
**用户授权**: 保留例子 `spec_create{name:"login"}`，字符上限 180→300（用户明确："这个长单独限制 可以弄300" + "不要为了限制而导致最后语义错误"）

---

#### `src/core/SpecManager.ts` (F-05)
**位置**: L215-219
**原文**:
```
Spec "XX" 已创建于 Scene "YY"，路径 ...
分流提醒（你正在开 spec，先确认该不该开、开在哪）：...
注意：Spec 创建成功后，不得擅自撤销或回退。建议下一步：...
请协助用户填充 requirements.md ...
```

**改后**: 三行结构【事实】→【建议】→【重要】
```
【事实】Spec "XX" 已创建于 Scene "YY"，路径 ...
【建议】如果这是已有特性的增量，通常可以考虑 context_search 找到对应 Spec 用 task_create 落位。判断时注意：
  - 整体推翻已有 requirements/design → 通常建议开新版（version+1）保留旧版对照
  - 独立且可验收的新特性 → 通常建议开新 Spec
  - 旧 Spec 出现上下文冷却信号 → 先读摘要再决定
  - scene 选择时，优先归入已有匹配业务域 scene；只有用户明确确认、或上下文非常清楚这是会承载多个 spec 的新业务域，才 scene_create；零散无稳定业务域的小型独立特性才落 00-default（兜底）。
【重要】以上是治理建议，不代表本次创建失败。若用户已明确要求创建独立 Spec，不得擅自撤销或回退。建议下一步：调用 context_search 查找相关需求，或问用户该 Spec 的具体范围。
请协助用户填充 requirements.md ...
```

**理由**: 
- 第一行【事实】前缀明确，符合 requirements L86
- 【建议】包含三标尺（整体推翻/独立特性/上下文冷却），符合 L87
- 【重要】包含"不代表本次创建失败" + "不得擅自撤销"，符合 L88
- 保留锚词 `context_search`/`00-default`，避免 spec-manager.test.ts:136 红

---

#### `src/core/SpecGuidance.ts` (F-06)
**位置**: L15-16
**原文**: 
```
若要整体推翻重做，建议开新版 spec_create --version（VV+1）保留旧版对照，再用 spec_update 归档旧版；只是增量加需求时在本版 task_create 即可，不必新开 spec。
```

**改后**:
```
若新需求整体推翻已有 requirements 或 design（而非增量补充），通常建议开新版（version+1），保留旧版供对照。只是增量加需求时在本版 task_create 即可。注意：以上是建议，若用户已明确要求独立 Spec（如"帮我新建一个 Spec"），即使已有相似 Spec 可以承载，也应尊重用户决定。
```

**理由**: 
- 明确"整体推翻"定义（与已有 requirements/design 方向相反）
- 补充用户决定优先条款

---

#### `src/core/GoalAssessor.ts` (F-07)
**位置**: L95-97
**原文**:
```typescript
if (kind === 'single-spec') {
  return '建议在现有 Scene 下创建一个 Spec，并把需求、设计、任务放入该 Spec。';
}
```

**改后**:
```typescript
if (kind === 'single-spec') {
  return '建议在现有 Scene 下创建一个 Spec，并把需求、设计、任务放入该 Spec。注意：suggested_next_step 是建议而非必须步骤，若用户已明确要求独立 Spec（如"帮我新建一个 Spec"），可直接调用 spec_create，无需等待 assess_goal 结果。';
}
```

**理由**: 与 multi-spec 分支的现有 override 对称

---

#### `tests/unit/tool-descriptions.test.ts` (F-10)
**位置**: L124-125
**原文**: `expect([...WORKFLOW_OVERVIEW].length).toBeLessThanOrEqual(600);`
**改后**: `expect([...WORKFLOW_OVERVIEW].length).toBeLessThanOrEqual(800);`
**注释**: `// 预算护栏：v2.3 扩容（+批量创建/治理地图/report 心智模型），上限 480→600；08-00 语义边界修复后 600→800；内容准确性优先于凑字数`

**位置**: L136-140
**原文**: `expect([...tool.description].length).toBeLessThanOrEqual(180);`
**改后**: `expect([...tool.description].length).toBeLessThanOrEqual(300);`
**注释**: `// 08-00 语义边界修复：spec_create 补充用户决定优先条款，上限 180→300`

**理由**: 用户授权放宽（语义完整性优先于字符限制）

---

### 3. 新增测试文件

#### `tests/unit/guidance-semantics.test.ts` (F-08)
**目的**: 语义回归扫描测试

**测试覆盖**:
1. **黑名单措辞**（不应出现强制语气）
   - `必须使用已有 Spec`
   - `禁止创建新 Spec`
   - `不新开 spec`
   - `默认不开 spec`
   - `是才开 spec`
   
2. **白名单条款**（应出现建议语气 + 用户决定优先）
   - WORKFLOW_OVERVIEW 包含"用户已明确" + "尊重用户决定"
   - spec_create 描述包含"用户已明确" + "尊重用户决定"
   - SPEC_REWRITE_GUIDANCE 包含"用户已明确" + "尊重用户决定"
   
3. **AI-ADAPTATION.md 与常量一致性**
   - 包含"用户决定优先" + "用户已明确要求" + "尊重用户决定"
   - 包含三条判断标尺（整体推翻/独立特性/上下文冷却）
   - 不含黑名单措辞

---

#### `tests/unit/mcp-server.test.ts` (F-09 追加)
**测试名称**: `F-09: 已有 Spec A 后仍可创建 Spec B（执行层非阻断）`

**测试覆盖**:
1. 创建 Spec A 成功
2. 创建 Spec B 成功（执行层不强制复用）
3. followup 不含强制措辞（"不应该新建" / "必须复用"）
4. followup 包含【重要】（不得擅自撤销）
5. 反向测试：两个 Spec 都存在于 spec_list

---

## 二、F-01 常量语义说明

### USER_DECISION_PRIORITY_CLAUSE 与 01-00 语义权威模型对应

**角色定义**:
- **USER_DECISION**: 用户明确表达的决定
- **RECOMMENDATION**: lrnev 提供的建议
- **EXECUTION_CONSTRAINT**: 真实系统约束（状态机/数据库唯一性/Git 冲突）

**优先级**: USER_DECISION > RECOMMENDATION（不可越过 EXECUTION_CONSTRAINT）

**来源限制**: USER_DECISION 只能来自：
- `user_quote`: 用户原话（如"帮我新建一个 Spec"）
- `client_asserted`: 客户端识别的用户意图

**不适用场景**:
- 状态机非法转换（如 `archived → in_progress`）
- 数据库唯一性冲突（如重复 Scene/Spec 名称）
- Git 操作约束（如未 commit 不能 push）

**常量文本**:
```
注意：上述建议不是强制规则。若用户已明确要求（例如"帮我新建一个 Spec"、"开一个独立的 Spec"），
即使已有相似 Spec 可以承载，也应尊重用户决定，直接调用对应工具（如 spec_create）。
```

**验证**: 常量未创造新的约束或改变执行逻辑，仅声明"建议不是规则、用户决定优先"

---

## 三、语义边界自查

所有修改后的文案均为 **RECOMMENDATION 语气**:

✅ **建议措辞**:
- "通常建议" / "通常可以考虑" / "通常适合"
- "若用户已明确" / "可直接"
- "以上是建议" / "不是强制规则"

✅ **唯一例外**（EXECUTION_CONSTRAINT）:
- "不得擅自撤销或回退"（防止 AI 自我回退的真实约束）
- "不代表本次创建失败"（事实陈述，非约束）

❌ **不含强制措辞**:
- "必须" / "不得" / "禁止"（除上述例外）
- "是才开 spec" / "默认不开 spec" / "不新开 spec"

---

## 四、测试结果

### 1. 全量测试
```
Test Files  61 passed (61)
Tests       773 passed (773)
Duration    20.93s
```

### 2. 编译验证
```bash
npm run build
> lrnev@2.3.0 build
> tsc
[无错误输出]
```

### 3. 语义回归测试（新增）
- ✅ 黑名单措辞不出现（5 个短语 × 3 个表面）
- ✅ 白名单条款出现（WORKFLOW_OVERVIEW / spec_create / SPEC_REWRITE_GUIDANCE）
- ✅ AI-ADAPTATION.md 与常量一致性

### 4. 执行层回归测试（新增）
- ✅ 已有 Spec A 后仍可创建 Spec B
- ✅ followup 不含强制措辞
- ✅ followup 包含【重要】（不得擅自撤销）
- ✅ 两个 Spec 都存在于 spec_list

---

## 五、红线验证

### 1. 不改执行逻辑
✅ 纯文案/常量/标尺修复
✅ 无 `spec_create` / `task_create` 代码行为改动
✅ 工具签名未变、MCP 协议未变

### 2. 02-00 基线不可变
✅ `guidance-surface-inventory-v2.json` 未修改
✅ `BASELINE-FREEZE-v2.0.md` 未修改
✅ v2 文件（`*-v2.*`）均未触碰

### 3. B0-s/B1/B2a 证据不动
✅ `b0-evidence-manifest.json` 未修改（git_sha 字段保持不变）
✅ `b1-evidence-manifest.json` 未修改
✅ `b2a-evidence-manifest.json` 未修改

---

## 六、授权偏离落档

### 已登记位置
`.lrnev/scenes/01-findings-remediation/specs/08-00-guidance-semantic-boundary/tasks.md` T-002

### 授权内容
**用户原话**（2026-09-01）:
- "这个长单独限制 可以弄300"
- "不要为了限制而导致最后语义错误"

**偏离项**:
- spec_create 描述字符上限 180→300
- 理由：语义完整性优先于字符限制，保留例子避免语义缺失

**连带调整**:
- tool-descriptions.test.ts 护栏 180→300
- WORKFLOW_OVERVIEW 护栏 600→800（USER_DECISION_PRIORITY_CLAUSE 插入是 F-03 需求本身）

**落档状态**: ✅ 已写入 T-002 任务描述，含授权日期 + 理由

---

## 七、文件变更清单

### 修改文件（8 个）
1. `src/core/guidance-semantics.ts` (新增，99 行)
2. `docs/AI-ADAPTATION.md` (L142, L150-155)
3. `src/mcp/guidance.ts` (L1-11, L28)
4. `src/core/SpecManager.ts` (L215-219)
5. `src/core/SpecGuidance.ts` (L15-16)
6. `src/core/GoalAssessor.ts` (L95-97)
7. `tests/unit/tool-descriptions.test.ts` (L124-125, L136-140)
8. `tests/unit/guidance-semantics.test.ts` (新增，97 行)
9. `tests/unit/mcp-server.test.ts` (L1098-1157 追加 F-09)

### 授权偏离落档
10. `.lrnev/scenes/01-findings-remediation/specs/08-00-guidance-semantic-boundary/tasks.md` (T-002 注记)

---

## 八、复审通过标准

### 必办项完成状态
- ✅ F-05: followup 三行结构（【事实】→【建议】→【重要】+ 三标尺 + 保留锚词）
- ✅ F-08: guidance-semantics.test.ts（黑名单 + 白名单 + AI-ADAPTATION 一致性）
- ✅ F-09: mcp-server.test.ts Case 1（已有 Spec A 后仍可创建 B + 反向测试 + followup 含【重要】）
- ✅ 授权偏离落档（T-002 tasks.md 注记）

### 验收标准
- ✅ 全量 773/773 测试通过
- ✅ 编译无错误
- ✅ 红线保持（02-00 基线不可变、执行逻辑不改、证据不动）

---

## 九、下一步（复审通过后）

1. Git commit（8 文件修改 + 2 测试新增 + 1 授权落档）
2. 更新 v3 标注（基于迁移后新文本）
3. 重新收集 B1-runtime 证据（基于新 guidance.ts）
4. B2a 重跑（基于新 v3）
5. M2/B2b 开发（可并行，但证据收集等 08-00 后）

---

**提交人**: Claude (Kiro)
**等待**: 用户复审通过后执行 git commit
