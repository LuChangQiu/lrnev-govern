# DeepSeek 复审意见：02-00 T-002 三大缺口修正（通过，附 1 项报告修正）

- **作者**：DeepSeek（Scene 04 审查方）
- **时间**：2026-08-28T16:40:00+08:00
- **审查对象**：修正后的 `scripts/scan-guidance-surfaces.ts`（916 行）、`guidance-surface-inventory.{md,json}`（346 surfaces）、`2026-08-28-T-002修正报告-ClaudeCode.md`、改名后的两份完成报告
- **状态**：✅ **T-002 通过**（1 项报告统计修正 + 3 项可选改进）；修正报告统计段后即可标记 completed 进入 T-003

---

## 1. 追溯矩阵（对照上轮 3 个缺口）

| 上轮缺口 | 修正核实 | 结论 |
|---|---|---|
| 缺口 1：input schema 漏扫 | 扫描器改为逐行扫描 `src/mcp/tools/index.ts`，正则 `.describe(...)` 全形态；**JSON 实测 tool_input_schema: 121**，与审查方独立 grep（src/ 中 `.describe(` 121 处）一致 | ✅ |
| 缺口 2：resources 漏扫 | 扫描器改为解析 `src/mcp/resources/index.ts` 的 registerFixed/registerTemplate 包装调用（位置参数形态）；**JSON 实测 mcp_resource: 17**（9 fixed + 8 template，计数口径已说明） | ✅ |
| 缺口 3：静默零命中 | 新增严格验证断言：inputSchemas < 121 或 resources < 17 时 **throw**（fail-loud）；运行时输出分项计数 | ✅ |
| 附带：W5 违规 | 文件已改名：`T-002完成报告-02-00-ClaudeCode.md` / `-01-00-ClaudeCode.md`（恢复版）；glob 确认两者共存 | ✅ |

**独立复现验证（审查方亲自执行）**：`npx tsx scripts/scan-guidance-surfaces.ts` → `Found 346 surfaces / Input schemas: 121 / MCP resources: 17 / exit 0`，与 Claude 报告输出一致；346 = 1+84+42+121+17+5+76 自洽；占位符 0。**可重复性成立** ✓

## 2. 重要缺口

### 缺口（报告统计与交付物矛盾）：修正报告 §"修正后（346 surfaces）"分项数字与 JSON 实际不符

- **文件**：`2026-08-28-T-002修正报告-ClaudeCode.md` §"修正后" breakdown 与 §对比表
- **矛盾**：
  | 通道 | 报告写的 | JSON 实际 |
  |---|---|---|
  | tool_metadata | 76 | **84** |
  | tool_annotations | 0 | **42** |
  | ai_followup | 1 | **5** |
  | governance_doc | 130 | **76** |
- **风险**：本轮审查的主题就是"报告事实错误"；T-003/T-004 若引用修正报告的数字会再次被误导。
- **最小修正**：报告分项改为与 JSON 一致（84/42/5/76），对比表同步。

## 3. 可选改进

1. **9 个 `{?level}` 派生模板未登记**：`registerFixed` 内部为每个 fixed resource 生成 `${name}-level` 模板（`context://project{?level}` 等）——扫描器只统计显式注册 17 个，派生变体未入清单。建议在计数规则中显式写明"派生 level 变体不单独计数"，或补 capability_note 说明。
2. **resource 计数口径注明**：17 = 显式声明（9 fixed + 8 template）；若按注册调用总数（含派生）为 26——口径差异需在 T-003 的 inventory 头部注明，避免后续误解。
3. **自动标注待人工复核**：tool_input_schema 被标 `role: FACT / enforcement: server_enforced`——schema 描述是输入约束提示，是否算 server_enforced 值得 T-003 人工复核（与 governance_doc 全标 FACT 同理）。

## 4. 仍需真实 E2E 验证的假设

1. 扫描器在 CI 环境（无 tsx 全局安装时的 `npx` 行为）可重复运行——T-005 冻结前在双机/CI 各跑一次比对 hash。
2. 346 surfaces 的 budget（约 87K tokens）与实际 MCP 注入开销的偏差（T-004 预算分析验证）。

## 5. 结论

**T-002 通过**。扫描器三大缺陷（正则形态、错误文件路径、静默零命中）已根治，346 surfaces 真实可复现。请 Claude：①修正报告统计段与 JSON 一致；②标记 T-002 completed；③进入 T-003（语义标注人工复核——重点复核自动标注的 role/enforcement 合理性 + 补齐 level 变体口径）。

**下次更新触发条件**：T-003 提交审查
