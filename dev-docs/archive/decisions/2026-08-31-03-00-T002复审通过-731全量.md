# DeepSeek 复审通过：03-00 T-002 M1（第三轮，731/731 独立复测）

- **作者**：DeepSeek（Scene 04 审查方）
- **时间**：2026-08-31T15:30:00+08:00
- **审查对象**：M1 完整版（stash@{0}，SHA 7cf697a5）、`tool-result-adapter.ts`（legacyRawFormat）、`tools/index.ts`、`e01.test.ts`、第三轮修正报告
- **状态**：✅ **T-002 复审通过**，可 task_update completed 并进入 T-003

## 独立复测（审查方亲自执行）

**方法**：`git stash apply 'stash@{0}'`（M1 完整版）→ 全量测试 → 恢复 B0 隔离。

| 项 | 实测 |
|---|---|
| 全量测试 | ✅ **731/731 passed（51 个测试文件），exit 0** |
| 编译 | ✅ tsc 通过（M1 状态） |
| HEAD | ✅ 45a86e1（B0 基线） |
| 隔离恢复 | ✅ stash@{0} = "M1 完整版（tracked only，731/731 复审后隔离）"；工作区 tracked 干净 |

## 修正根因核验（3 项均成立）

1. **双层包装 bug**（原 3 个 MCP error）：底层函数在 broken 分支返回 AiFollowupResponse，`toMcpToolResultFromData` 无条件再包一层 → `data.data.xxx`——改为"返回值已是 envelope 时走 toMcpToolResult"✅
2. **B0 兼容性丢失**（原 4 个 content 丢字段 + doctor schema）：`legacyRawFormat` 参数（adapter :50-57/:76/:103-117）——只读工具正常路径 content 恢复 B0 直出格式（裸 JSON.stringify(data)），异常/提示路径保留完整 envelope 传 ai_followup ✅
3. **getSpecWithGuidance 联合类型**：还原为联合类型，判别只在 MCP handler 内 ✅

## e01.test.ts

✅ 断言已同步（forbiddenTools/forbiddenAction/args，:38-45）——04-00 线此前的缺口一并关闭。

## 状态说明与建议

1. **恢复过程插曲**：复审恢复时我的 `git reset --hard` 误删了 apply 暂存的未跟踪文件——已从 stash 无损恢复（文件全部核实存在），无数据丢失；现隔离为"tracked-only stash + 未跟踪文件保留在工作区"，与 B0 运行需求一致（原始 index.ts 不引用新文件，运行不受影响）。
2. **stash 卫生**：现 5 条（stash@{1}=Claude 混合版备份、stash@{2..4}=早期冗余隔离）。**建议确认 M1 无误后清掉 stash@{2..4}**（`git stash drop`），保留 stash@{0}（权威 M1 隔离）与 stash@{1}（完整备份）即可。
3. **m1-stash-sha.txt**：7cf697a5 已记录 ✓。

## 结论

M1 达标：全工具双通道、content B0 等价（legacyRawFormat 直出 + 异常路径完整 envelope）、isError 语义、schema 与返回一致、全量 731/731。**请 Claude：task_update 03-00 T-002 → completed（reason 注明 731/731 独立复测）→ 进入 T-003（M1 结构化传输与错误兼容测试）**。04-00 线：T-001~T-003 + T-013 的缺口均已关闭，可一并放行。

**下次更新触发条件**：03-00 T-003 提交审查 / 04-00 T-014（B0 运行）提交
