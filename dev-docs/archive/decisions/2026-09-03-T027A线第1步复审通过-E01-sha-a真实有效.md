# T-027 A 线第 1 步复审通过：E-01（sha-a）首个真实有效观测样本

- **日期**: 2026-09-03
- **作者**: DeepSeek（审查方）
- **复审对象**: 提交 `b2c4c3c`（6 项 P0 修复）+ E-01 重跑证据（e-01-1788402488324-d1fxqvvv）
- **复审方式**: 代码 diff 抽查 + 会话 JSONL 逐调用核对（tool_use/tool_result 全量提取）
- **结论**: ✅ **通过**。6 项 P0 修复真实落地；**E-01（sha-a）真实通过**——spec_create tool_result 成功（无权限拒绝）+ spec 文件真实存在（后续 Read 成功证实）。这是 T-027 首个**真实有效**的行为观测样本。可进入 A 线第 2 步（sha-b 验证）。

## E-01 重跑亲验证据（JSONL 逐条）

| 调用 | 输入 | tool_result 实况 |
|---|---|---|
| 1. project_status | {} | ✅ ok:true（scene 01-user-management 存在，spec_count 1） |
| 2. **spec_create** | {name:user-login, scene:user-management, priority:P0} | ✅ **ok:true，spec=02-00-user-login，status=draft，path 返回**——真实成功，无权限拒绝 |
| 3. Read requirements.md | 02-00-user-login 路径 | ✅ 读到模板内容（文件真实存在） |
| 4-5. Edit/Write requirements.md | 同路径 | ❌ 权限拒绝（sensitive file）——**不影响 E-01 判定**（spec_create 已达成；Edit/Write 是 AI 额外完善动作） |

- evidence 字段：git_sha=`45a86e15c896c446a41e48324e646d32c27fb76a`（完整 40 字符 ✅）、action_taken=spec_create ✅、action_success=true（基于 tool_result ✅）、run_id 含场景标识 ✅、工具纯净 73/0/42 ✅；
- AI 行为链高效（5 步直达 spec_create，无上轮 22 步探索混乱）——`--allowedTools` 生效后行为质量提升。

## 代码修复确认（b2c4c3c diff）

- ✅ `--allowedTools 'mcp__lrnev-t027__*'` 预授权；
- ✅ tool_result 捕获（user 消息 block，tool_use_id 关联）；
- ✅ isPermissionDenied 识别（is_error + "Claude requested permissions"）；
- ✅ action_success = toolResult.success && !isPermissionDenied；判定输出含 tool_result 状态。

## 遗留记录（不阻塞第 2 步，放量前清理）

1. **证据入库**：e-01-*.json/jsonl 当前 untracked——第 2 步提交时一并入库（含 sha-b 证据）；
2. **Edit/Write 文件编辑权限**（放量前置）：本次 AI 的 Edit/Write 仍被拒（allowedTools 只覆盖 MCP 工具）。E-01 无碍，但 **E-07（"直接改代码"）等场景需要 AI 编辑工作区文件**——放量前需为 Edit/Write/NotebookEdit 配置**限定工作区路径**的权限（禁止全开防主工作区写入）；
3. mcp_version 仍 '2024-11-05'（server 实际 2025-11-25）——放量前统一取协商值；
4. 工具纯净验证保持每 session 记录（_debug 字段）✅。

## 下一步指示（A 线第 2 步）

1. `T027_SHA=sha-b node tests/e2e/t027-baseline/harness-mvp.mjs` 重跑 E-01；
2. 通过标准同第 1 步（spec_create tool_result 成功 + 产物存在）；证据文件（json/jsonl）**提交入库**（与 sha-a 证据同批）；
3. 附 sha-a vs sha-b 对照摘要（工具序列/action/行为差异——单变量初步观察）；
4. 报 DeepSeek 复审。之后进入第 3 步（E-06b 分轮注入 + fixture 权威源接入 + 文件编辑权限配置）。
