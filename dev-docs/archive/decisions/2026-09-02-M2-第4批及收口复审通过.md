# M2 第 4 批及完整收口复审通过

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-02
- **复审对象**: 第 4 批（44e5afe + f4511b9 + e9e0cda）+ M2 完整收口（42/42）
- **结论**: ✅ **M2 验收通过**，附 1 项必办（B2b 证据重生成）

## 一、实测结果

| 项 | 结果 |
|---|---|
| **独立复跑** | ✅ **855/855**（65 files，21.48s） |
| 42/42 渲染器 | ✅ 全部注册（4 批：9+18+8+7） |
| 测试修正 diff | ✅ batch4-A 是**适配 markdown 加粗格式**（`agent-a` → `**agent-a**`），非删断言——偷懒驳回无效，正当适配 |
| 渲染器质量抽查 | ✅ governance-map（scene→spec→anchors 完整层级+status+priority）、lrnev-report（欠债清单 4 类+validates 覆盖率+next_action 完整） |
| 投影原则 | ✅ 无 paraphrase、无自创 guidance |
| B2b 证据 | ⚠️ 12 条完整，但 **git_sha=f4511b9 ≠ HEAD=e9e0cda**（见必办） |

## 二、✅ 统一逃逸层设计验证（关键结论）

**renderModelVisibleContent（L103）对所有渲染器输出统一调用 escapeFrameworkMarkers**——这是 D-04.1 的正确实现：

1. **统一逃逸 = 设计正确**：lrnev-report/governance-map 等渲染器内部不逃逸是对的（避免破坏统一层）
2. **第 2/3 批内部手动逃逸 = 冗余但无害**：幂等性实测 `escape(escape(x)) === escape(x)` ✅——不会双重转义
3. **无 prompt injection 漏洞**：所有 content 出口经统一逃逸 ✅

**建议（非必办）**：后续可清理渲染器内部冗余 escape（如 error-record/memory-save 的 import+调用），保持单一逃逸路径——但当前幂等兜住，不阻塞。

## 三、❌ 必办 1 项：B2b 证据需重生成（git_sha 过期）

- **实测**：B2b manifest git_sha=f4511b9（测试修正 commit），但 HEAD=e9e0cda（证据生成 commit）
- **含义**：B2b 在 e9e0cda 之前生成，证据可能基于未完全最终代码（虽然 f4511b9 只是测试修正+doctor 空值防护，不影响 11 场景证据，但 git_sha 必须反映生成时刻）
- **必办**：`npx tsx scripts/run-b0-baseline.mts --stage=B2b` 重跑（HEAD=e9e0cda），确认 12 条 + git_sha=e9e0cda

## 四、M2 完整收口确认

| 项 | 状态 |
|---|---|
| 42/42 渲染器（100%） | ✅ |
| 855/855 测试 | ✅ |
| MVC required 字段 | ✅（D-04 五类全覆盖） |
| 统一逃逸层 | ✅（D-04.1 正确实现） |
| legacy 回退 | ✅（未注册工具 → legacyJsonRenderer） |
| 08-00 常量单源 | ✅（无 paraphrase） |

## 五、放行（M2 后链路）

1. **必办**：B2b 证据重生成（git_sha=e9e0cda）
2. **03-00 落库**：T-004（MVC 定义）/T-005（渲染器实施）/T-006（M2 验证+B2b 对照）→ completed；**T-003（M1 协议测试）补做落库**（遗漏项）
3. **04-00**：T-023（B2b 测试套件）→ T-024（B2b 证据）→ T-025（B0/B1/B2a/B2b 综合对比）→ T-026（最终报告）
4. **收尾清单**：renderers.test.ts 拆分（TODO）
5. **T-027**（真实客户端基建后）→ 05-00 → 06-00 → T-029

## 六、红线保持

- 02-00 基线不可变、08-00 常量单源、M1 legacy 保留
- 证据禁改写、报告≠落库（855/855 已独立复跑）

## 七、M2 里程碑总结

**M2 = content 切 ModelVisibleContract 完成**：42 工具逐类渲染器、统一逃逸层、按工具回退、855 测试、B2b 证据。B2a（M1 双通道）→ B2b（M2 模型视图）对照链完整。
