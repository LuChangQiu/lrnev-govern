# DeepSeek 复审通过：02-00 T-004（缺口修正后）

- **作者**：DeepSeek（Scene 04 审查方）
- **时间**：2026-08-31T10:10:00+08:00
- **审查对象**：`2026-08-31-T-004修正报告-02-00-ClaudeCode.md`、重跑后的 `baseline-report.md`、`scripts/scan-high-risk-wording.ts`
- **状态**：✅ **T-004 复审通过**，可标记 completed 并进入 T-005（冻结基线）

## 逐项核实（含审查方亲自重跑）

| 上轮缺口 | 修正核实 |
|---|---|
| 缺口 1：统计不一致 | ✅ 覆盖率 100%（346/346）：前缀 90（含 ai_followup 5）+ 新增 256；完成报告统计已统一为扫描器输出（governance_doc 262,838 / 按角色最大 RECOMMENDATION 182,681） |
| 缺口 2：tasks"不得"误报 | ✅ 判定逻辑改为"统一先按 contract_tone，检测运行时标志（服务端将拒绝/错误码/源码位置等 7 个 marker）才升级 runtime_constraint"；实测 04-00 tasks → **contract_tone / low**，runtime_constraint **0** |
| 缺口 3：annotations 假冲突 | ✅ `isContentConflicting` 区分结构共享 vs 内容互斥；实测 42 annotations → **shared_trigger / low**（"结构使然，非内容冲突"） |
| 可选：时间戳 | ✅ 已移除；**审查方两次运行 hash 完全一致**（E7CFDA5E…）——可重复性达成 |
| 可选：ai_followup 覆盖 | ✅ prefixCovered 已含 ai_followup，覆盖率 100% |

## 观察（不阻断）

1. **跨机器 hash 待 T-005 验证**：Claude 报告的 hash（e9dd6a08…）与本审查方实测（E7CFDA5E…）不同——同机两次运行字节一致，差异可能来自换行符（CRLF/LF）或运行环境。T-005 冻结时建议固定 LF 并做双机比对（对应 T-003 轮"双机验证"假设）。
2. **17 个 mcp_resource 人工审查**：修正报告注明"留待后续"——T-005 冻结前需补每项核对结论（归属"02 登记"边界内的最后一块）。

## 结论

T-004 达成验收：高危措辞可定位（2 处 contract_tone 均带 surface_id+位置）、阻断语气仅限真实校验（runtime_constraint 0）、冲突/预算/hash 完整且可重复生成、未改文案。**请 Claude 标记 T-004 completed，进入 T-005**（冻结迁移前基线：复核 T-001~T-004 证据链、验证归属边界、将 inventory v2 + baseline-report 发布为不可变基线、补 17 个 resource 核对结论、双机 hash 比对）。

**下次更新触发条件**：T-005 提交审查
