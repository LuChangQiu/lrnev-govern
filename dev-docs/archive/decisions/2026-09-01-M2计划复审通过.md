# M2 计划补充复审通过——Phase 1 放行

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-01
- **复审对象**: .claude/plans/m2-model-visible-contract-implementation.md（627 行）补充版
- **结论**: ✅ **补充通过，Phase 1 放行**

## 一、必办 4 项 + 说明 3 项落实核验（亲读计划文件）

| 项 | 实测 | 判定 |
|---|---|---|
| ① 逃逸层级契约 | ✅ escapeFrameworkMarkers 9 处（含幂等性契约 `escape(x)===escape(x)` 设计） | ✅ |
| ② Canonical 单源 | ✅ canonical 9 处（渲染器统一从 payload 取值 + 双通道一致性测试） | ✅ |
| ③ 体积控制同步 | ✅ truncated 2 处（截断元数据先写 payload，两通道同步） | ✅ |
| ④ 错误路径 MVC | ✅ 错误路径 9 处 + isError 5 处（schema 失败不回退旧文本、isError=true 仍满足 MVC） | ✅ |
| 说明 1 B2b hash 口径 | ✅ 明确：content_hash 基于 MVC 渲染结果（renderModelVisibleContent），B2a vs B2b 必然不同但 action_success 12/12 保持 | ✅ |
| 说明 2 JSON.parse 迁移 | ✅ 8 处（约 50-80 用例迁移到 structuredContent + 快照） | ✅ |
| 说明 3 交集工具优先 | ✅ 第 1 批 = guidance 交集 11 工具（验证 M2 不绕过 08-00 迁移） | ✅ |

## 二、Phase 1 放行条件

**已满足**：
1. ✅ 逃逸实现 + 安全测试（幂等性）
2. ✅ canonical payload 单源 + 一致性测试
3. ✅ truncated 两通道同步规则
4. ✅ 错误路径 MVC 测试
5. ✅ 补充说明落计划文档

**放行**：开始 Phase 1（基础设施搭建：model-visible-contract.ts + renderers/ + escape.ts + 测试框架）。

## 三、实施红线（每批验收）

1. **每批独立验证**：build + 全量测试（773 基线）+ 该批 B2b 证据 → 提交复审 → 通过后下一批
2. **按工具回退**：任何工具退化 → 只回退该工具到 M1 renderer（记录丢失/误读字段），不全局开关
3. **旧 JSON.parse(content) 测试同步迁移**：不得保留双套断言真相（T-005 L72）
4. **08-00 guidance 不被绕过**：第 1 批（11 交集工具）先验证渲染包含 USER_DECISION_PRIORITY_CLAUSE
5. **红线**：02-00 基线不可变、M1 legacy renderer 保留（回退目标）、证据禁改写

## 四、复审节奏

- Phase 1（基础设施）完成 → 提交复审
- 第 1 批（11 交集工具）完成 → 提交复审（重点：08-00 文本不绕过 + B2b hash 口径生效）
- 第 2-6 批按类推进 → 每批复审
- 全部完成 → T-023~T-026（B2b 证据 + 综合对比）→ 最终复审

**红线保持**：02-00 基线不可变、08-00 迁移成果不被渲染器绕过、M1 renderer 保留、报告≠落库。
