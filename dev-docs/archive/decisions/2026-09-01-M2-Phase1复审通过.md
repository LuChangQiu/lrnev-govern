# M2 Phase 1 复审通过

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-01
- **复审对象**: M2 Phase 1（基础设施 + 46/46 toolName + 测试迁移）
- **结论**: ✅ **验收通过**，放行第 1 批

## 一、独立验证结果

| 项 | 结果 |
|---|---|
| 基础设施 4 文件 | ✅ model-visible-contract.ts（注册表 Map/registerRenderer/renderModelVisibleContent/escapeFrameworkMarkers 幂等性契约 L108）/ renderers/legacy.ts / renderers/error.ts / tool-result-adapter.ts |
| toolName 必填 | ✅ 46/46 调用全部带（跨行/第三参数 true 均确认） |
| **全量测试独立复跑** | ✅ **788/788**（62 files，20.49s） |
| 04-00 证据测试 | ✅ 43/43（M2 未破坏证据链） |
| 单元测试 | ✅ 15/15（逃逸幂等/双通道一致/错误 MVC） |
| 测试迁移 | ✅ 9 处 JSON.parse → structuredContent（错误路径直接访问 / 成功路径 .data） |
| legacy 回退 | ✅ legacyJsonRenderer = M1 完整 JSON（稳定回退目标） |

## 二、Phase 1 交付质量

1. **MVC 架构符合 D-04**：注册表驱动、未注册→legacy 显式回退、escapeFrameworkMarkers 幂等
2. **toolName 必填消除静默降级**：编译期保证 46/46 全覆盖（策略裁定落实）
3. **测试迁移符合 F-09**：机器断言→structuredContent、不保留双套真相；legacyRawFormat 工具（scene_list 等）走 legacy 分支 JSON 保留
4. **错误路径 MVC**：L525/L541 错误路径直接访问 structuredContent（含 ok/errors/ai_followup）

## 三、放行：第 1 批（11 个 guidance 交集工具）

**第 1 批范围**（08-00 迁移交集工具）：spec_create / task_create / spec_get / spec_list / spec_update / spec_gate_check / task_create_many / task_update / assess_goal / context_search / scene_create（以 04-00 11 场景 fixture 实际引用为准）

**第 1 批验收**：
1. 为 11 个工具注册 MVC renderer（write-state-change / selection / list-search / gate-validation / confirmation 分类）
2. **验证 08-00 迁移文本不被绕过**：渲染 content 含 USER_DECISION_PRIORITY_CLAUSE（workflow_overview）与【事实】→【建议】→【重要】结构（spec_create followup）
3. B2b 证据采集（--stage=B2b）：content_hash 基于 MVC 渲染结果，与 B2a 不同但 action_success 12/12 保持
4. 每批独立：build + 788 全量 + 证据 → 提交复审
5. 按工具回退验证（注册 renderer 后故意触发退化→单工具回退 legacy，不全局开关）

## 四、红线保持

- 02-00 基线不可变、08-00 迁移文本不被渲染器绕过（第 1 批核心验证点）
- M1 legacy renderer 保留（回退目标，已验证）
- 报告≠落库（以实测 + 独立复跑为准）

## 五、Phase 1 提交要求

- git commit（基础设施 + adapter + tools/index + 测试迁移）
- 提交信息注明"Phase 1：MVC 基础设施 + 46/46 toolName + 9 处测试迁移（788/788）"
