# B0-s fixture_hash 修复复审通过

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-01
- **复审对象**: current_status 纳入 fixture_hash（上轮必办残留）
- **结论**: ✅ **B0-s 全绿，验收通过**

## 一、实测结果（独立复验）

| 项 | 结果 |
|---|---|
| **current_status 敏感性** | ✅ 独立重算：archived=`852bc0d5…`、completed=`a9ede3dc…`，**from 端改动可检测**（修复前 9aaa3613 无感，漂移漏报已消除） |
| **12 条对照** | ✅ 独立重算 vs manifest 存档 **12/12 全部一致** |
| **E-08 判定保持** | ✅ action_success=false / failure_category=state_machine_validation（未受 hash 变更影响） |
| git diff fixtures/e2e | ✅ 空 |
| git HEAD | ✅ 45a86e15c896c446a41e48324e646d32c27fb76a |
| vitest | ✅ 43/43 |
| 生成器 | ✅ scripts/run-b0-baseline.mts（--stage 参数化，可复现） |
| 报告 | ✅ 含 B0-s 横幅 + F-01~F-08 + 修复日志 + hash 对照附录 |

## 二、修复质量确认

1. **粒度正确**：追溯 decisionContext 全部使用点，确认仅 `current_status` 参与判定（其余 9 个子字段在采集器零匹配），只纳入一个字段而非整个对象 ✅
2. **方法安全**：内存克隆（JSON.parse/stringify）做负向验证，不碰文件（上轮"改文件再改回"的隐患消除）✅
3. **判定与 hash 一致**：判定输入（from 端 current_status + to 端 expectedArgs.status）与 hash 输入完全对齐 ✅

## 三、B0-s 最终状态

| 项 | 状态 |
|---|---|
| 12 条证据 | ✅ 自洽，action_success/failure_category 与 fixture 声明一致 |
| fixture_hash | ✅ 语义准确（12 行为字段含 from/to 两端）+ 跨进程稳定 |
| 生成器 | ✅ 落库，--stage 参数化 |
| 报告 | ✅ 诚实声明完整（B0-s 横幅、非行为基线、tool_sequence 回放、限制） |
| T-014~T-017 | ✅ 已落库 completed（reason 含 B0-s 限定） |

## 四、放行决定

1. **B0-s 验收通过**——采集链路、数据契约、版本追溯、可复现性全部就绪
2. **B1（08-00 文本迁移）可放行**——行为对照已由"双 SHA 对照"方案覆盖（B1 后真实客户端分别跑 45a86e15 与 B1 SHA，同一客户端/模型只变文本变量，符合 D-04 单变量原则）
3. **M1（stash@{0}）仍锁**——M1 属 03-00 通道改造，与 B0/B1 无依赖冲突但按序推进，B1 完成后再评估
4. **B0' 前瞻**（已记录）：真实客户端接入后，userInput/decisionContext 全字段必须重审 hash 分类（届时都是行为输入）

## 五、两项待定（最终建议）

1. **客户端选型**：Claude Code 主 + Codex 补 F-04 双客户端门禁（各 ≥1/5 clean sessions）——同意
2. **B0' 并入 T-027**：建议并入，T-027 acceptance 扩写为"行为基线 B0'/B1'（双 SHA 对照）+ 盲测（≥5 clean sessions）"双产出——同意

待用户确认这两项后，B0 阶段正式收口。
