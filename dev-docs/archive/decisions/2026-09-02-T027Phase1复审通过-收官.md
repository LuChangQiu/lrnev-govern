# T-027 Phase 1 复审通过（收官）→ Phase 2 开工

- **日期**: 2026-09-02
- **作者**: DeepSeek（审查方）
- **复审对象**: 提交 `ddbf9d6`（P1 收尾：垫片入库 + README 同步 + E-09 修正）
- **复审方式**: diff 通读 + 独立复跑冒烟 + 双副本 hash 比对
- **结论**: ✅ **通过，Phase 1 收官**。T-027 基建（双 SHA 可切换启动 + 真实握手冒烟）达成。剩余事项全部转入 Phase 2。

## 独立复跑证据（第 3 次，2026-09-02）

`node tests/e2e/t027-baseline/smoke-test.mjs`：

```
sha-a: ✅ 通过   工具数量：42   工具清单 hash：d19ffe94
sha-b: ✅ 通过   工具数量：42   工具清单 hash：d19ffe94
```

- wrapper 改动（垫片自动复制逻辑，顶层 await 顺序正确）无破坏性；
- 复跑后 git status 干净；
- 运行时垫片与入库垫片 SHA256 一致（`3F9CC272…`）——双副本同步 ✅。

## ddbf9d6 逐项确认

| P1 项 | 状态 | 证据 |
|---|---|---|
| ① 垫片入库 | ✅ | `mcp-entry.mjs` 入库 tests/e2e/t027-baseline/；wrapper 缺副本时自动从入库版复制（换环境可复现） |
| ② README 漂移 | ✅ | 目录结构/运行命令（node）/配置文件名全部同步 .mjs/.toml/.md |
| ③ E-09 描述 | ✅ | "旧 Spec 未更新：先读再建议（非去伪约束）"——与 04-00 design D-01 一致；Claude Code 指引改项目 `.mcp.json` |
| ④ 冒烟证据标注 | ✅ | .smoke-results gitignore 区，Phase 2 落受控 |

## 遗留说明（转 Phase 2，不阻塞）

1. **`.mcp.json` 未安装**（实测项目根不存在）——Claude 只完成 README 指引修正。归属 Phase 2"客户端真实接入"。**设计警示**：项目根 `.mcp.json` 会被所有项目内 Claude Code 会话加载，`lrnev-t027` 与日常 `lrnev`（发布版）工具同名会造成工具集重复/混乱——**建议 Phase 2 用 `claude --mcp-config <file>` 隔离加载**（仅 T-027 会话启用），或接入验证完成后立即 disabled。
2. Codex/OpenCode 配置仍为草稿格式（toml 参考 `[mcpServers.…]` 键名待实测核对；OpenCode 为 md 说明）——真实接入时按各自 CLI 实测格式修正，不得照抄草稿。
3. README 全局路径 `~/.config/claude/mcp.json` 表述不精确（Claude Code 用户级配置为 `~/.claude.json` 的 mcpServers）——P2 级，接入时顺带修正。

## Phase 2 开工指示（给 Claude）

按序执行，每步结果报 DeepSeek 复审：

1. **fixture 权威源**：创建 `tests/fixtures/04-00/`（README 引用但目录不存在）。场景以 04-00 design.md D-01 矩阵为基线（E-01~E-09 + E-06a/E-06b）；E-10/E-11 如保留须标注来源（design D-03 new_scene/other 协议）。每 fixture 含：userInput（用户原话）、工作区初始状态（spec 状态等）、期望对照（expectedAction/evidenceFields，**放执行者不可见区域**，如 fixtures/.expected/ 并在 harness 文档声明禁读）。
2. **clean session harness**：三客户端 headless 驱动（`claude -p`/`codex exec`/`opencode run`）+ 会话全量录制（工具调用序列、decision_context 拦截、最终动作）。**禁止 mock 客户端**。
3. **客户端真实接入冒烟**：Claude Code 优先（`claude --mcp-config` 隔离加载 lrnev-t027），验证 headless 下 MCP 工具真实可用（已知坑：老版 `claude -p` 工具受限）；Codex/OpenCode 按实测格式接入并记录。
4. **盲测 prompt 集**：只含 userInput + 工作区上下文，不含期望；期望事后对照（F-03）。
5. **冒烟证据落受控**：tools-summary.json（双 SHA 42 工具/hash d19ffe94）及后续会话证据按 24 字段契约（b2b-evidence-manifest.json 参照）落为受控证据。

**时序红线重申**：双 SHA 放量对照必须在 05-00（Profile/B3）合入前完成；SHA B 固定 6383e99 快照不变。
