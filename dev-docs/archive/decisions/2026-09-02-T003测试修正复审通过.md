# T-003 测试修正复审通过——测试合格，3 个真问题需修实现

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-02
- **复审对象**: commit 220c4c0（T-003 测试修正）
- **结论**: ✅ **测试合格**（实质验证、非空转），3 个失败确认真问题——**T-003 可先落库（测试暴露问题），P0 修复另立任务**

## 一、测试修正验收（独立复跑）

| 项 | 结果 |
|---|---|
| **独立复跑** | ✅ 3 failed / 11 passed（14 tests）——测试实质有效 |
| 测试 bug 修复 | ✅ 4 个（lrnev_ 过滤、annotations 字段名 Hint 后缀、内部错误期望、业务拒绝场景构造） |
| 42 全量断言 | ✅ `toolsList.tools.length === 42`（我独立实测 42 ✅） |

## 二、3 个失败确认真问题（独立核实）

### P0-1：18/42 工具缺 outputSchema ✅ 属实
- 我独立块级扫描：42 注册工具中 **18 个块无 createToolOutputSchema**
- 清单：adr_create/adr_list/adr_get/assess_goal/summarize_save/context_search/error_record/error_search/error_promote/memory_save/memory_search/memory_forget/lrnev_doctor/lrnev_hook_list/lrnev_hook_trigger/lrnev_hook_tail_log/lrnev_hook_enable/lrnev_hook_disable
- **影响**：M2 渲染器已注册这些工具，但 outputSchema 缺失 → structuredContent 无 schema 校验

### P0-2：SpecDataSchema 额外属性 → -32602 ⚠️ 需复现确认
- SpecDataSchema 结构正常（z.object 默认 strip 额外属性）
- **需复现**：哪个调用路径产生额外属性导致 -32602（可能 spec_create 传了 schema 外字段）

### P1：AMBIGUOUS_REF candidates ⚠️ 部分实现
- SpecManager L308/L324 抛 AMBIGUOUS_REF + adapter L142 处理 + error.ts candidates 字段——**实现存在**
- 测试失败原因需定位：E2E 路径未通（可能 candidates 未填充或响应结构不符）

## 三、裁决：T-003 落库 + P0 另立任务

**T-003 的职责是"建立协议测试体系"**——测试合格（能暴露真实问题）即达成。**3 个问题属于实现缺陷，不属于 T-003 阻塞项**：

1. **T-003 → task_update completed**（reason：`协议测试体系建立，14 测试实质验证（11 通过 + 3 暴露真问题），42 工具全量断言`）
2. **P0-1（18 工具缺 outputSchema）→ 立修复任务**（T-003 发现，03-00 后续实施）
3. **P0-2（-32602）/ P1（AMBIGUOUS_REF）→ 并入修复任务复现定位**
4. 修复完成后：全量测试 869+ → 3 个失败转绿 → T-004/T-005/T-006 落库

## 四、下一步

```
1. T-003 落库 completed
2. 立 P0 修复任务（18 outputSchema + -32602 + AMBIGUOUS_REF）→ 复审 → 修实现
3. 修完后 T-004/T-005/T-006 落库
4. 04-00 T-023 → T-024 → T-025 → T-026
5. renderers.test.ts 拆分 → T-027（客户端基建）→ 05-00/06-00/T-029
```

## 五、红线保持

- 02-00 基线不可变、08-00 常量单源、M1 legacy 保留
- git_sha 不得手改、报告≠落库（3/11 已独立复跑）
- **测试不得空转**（本次教训已固化：条件跳过/空循环/自说自话一律拒）
