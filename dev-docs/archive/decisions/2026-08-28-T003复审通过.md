# DeepSeek 复审通过：T-003（缺口修正后）—— 首份含追溯矩阵的审查报告

- **作者**：DeepSeek（Scene 04 审查方）
- **时间**：2026-08-28T14:30:00+08:00
- **审查对象**：修正后的 `tests/unit/semantic-authority-model.test.ts`（547 行）+ `ai-discussions/结果/2026-08-28-T-003缺口修正报告-ClaudeCode.md`
- **状态**：✅ **T-003 复审通过**，可标记 completed 并进入 T-004
- **方法论**：按 `deepseek/05-review-methodology.md` 协议执行（首份含追溯矩阵的报告）

---

## 1. 追溯矩阵

| 需求 F-xx | 设计 D-xx | 任务验收 | 产出物证据 | 核验方式 | 结论 |
|---|---|---|---|---|---|
| F-05 五类案例闭环 | D-05 五类案例 | T-003 验收1：覆盖用户故事与 D-05 边界 | fixtures E-01~E-05（各含 scenario/clientBehavior/serverBehavior/observableResult/userStory） | 读 fixture + 测试断言 | ✅ |
| F-05 约束冲突只引真实约束 | D-07 固定 fixture | T-003 验收3 | E-05 `realConstraintReference`（inventory §1.1、spec.ts:25-31、SpecManager.ts:136-158、VALID_SPEC_TRANSITIONS、archived 终态） | 逐字段对照源码（T-002 已核验事实） | ✅ |
| F-05 静态检查定位禁止语义 | D-07 静态审查（建议非阻断/来源可追溯/边界不越权/约束可回指/三维不扩散） | T-003 验收2 | `checkSemanticViolations(text)`（3 条规则：建议强制语、非用户来源 USER_DECISION、边界伪装）+ 3 组测试正反例 | 读函数 + 实测 | ✅（见可选改进 1/2） |
| F-05 不要求三维必填字段 | D-07 | T-003 验收2 后半 | 三维框架非扩散测试（fixture 结构断言） | 读测试 | ⚠️ 弱断言但可接受（见可选改进 3） |
| 前序遗留：assertValidatesAnchors 覆盖 | — | DeepSeek 两轮要求 | 7 个直接测试（有效 F/D、缺失 F/D、混合、非法格式、legacy design#） | 读测试 + 实测 | ✅ |
| 前序遗留：空断言测试 | — | 上轮缺口 1 | `expect(fixtureText).not.toContain(nonConstraint)`（:542） | 读代码 | ✅ |
| 前序遗留：恒真断言 | — | 上轮缺口 2 | 4 处已删，改为 `checkSemanticViolations` 断言 | 读代码 | ✅ |
| 测试真实可运行 | — | — | `npx vitest run` → **20 passed (20)，286ms** | 审查方亲自运行 | ✅ |

## 2. 逐项核实

- **缺口 1**：`:527-545` 现含真实断言 `expect(fixtureText).not.toContain(nonConstraint)`，对全部 5 个 fixture 生效——假约束引用会被拦截 ✓
- **缺口 2**：`checkSemanticViolations`（报告称位于 :25-60）返回 `{pattern, location, violation}` 三元组，支持 3 类禁止模式；3 组静态检查测试已改用该函数并断言违规定位；4 处恒真断言已删除 ✓
- **实测**：修正后 20/20 通过（286ms），与修正报告一致 ✓

## 3. 可选改进（不阻断）

1. **checkSemanticViolations 覆盖 6 条 §8 规则中的 3 条**：缺"决定来源可追溯"（user_quote/client_asserted 必须可关联）与"决策边界不越权"（`【决策边界】…服务端将拒绝` 模式）。T-004 或 02-00 阶段可扩展（02-00 的 F-03 高危措辞扫描正是这个函数的用武之地）。
2. **正则小瑕疵**：`/(GoalAssessor|spec_list|gate check|tool result)[^。！？]*?【用户已决定|用户确认】/gi` 的括号位置使 `】` 只挂在第二分支——当前测试能过，建议改为 `【用户已决定】|【用户确认】` 更清晰。
3. **三维框架非扩散测试**（:466-505）仍是"断言 fixture 有字段 + 记录反模式"的弱断言——可接受（fixture 结构回归用），后续可改为断言"检查函数不把三维字段列为必填"。
4. **"User Decision Above Real Constraint" 测试**（:370-390）未使用定位器，是数据形状断言——建议并入 checkSemanticViolations 的第 4 条规则。

## 4. 仍需真实 E2E 验证的假设

1. 五类案例在真实客户端（Claude Code / Codex）的行为符合 fixture 客户端行为描述（04-00 B0/B1 验证）。
2. `checkSemanticViolations` 对真实 guidance 文案全量扫描的误报/漏报率（02-00 F-03 高危措辞扫描引入时验证）。

## 5. 结论

T-003 达成验收：五类案例覆盖用户故事与 D-05 边界 ✅、静态检查可定位禁止语义（3/6 规则，余 3 条列入可选改进）✅、约束冲突只引真实约束 ✅、`assertValidatesAnchors` 直接测试补足 ✅、测试实测通过 ✅。**请 Claude 标记 T-003 completed，进入 T-004**（冻结 v0.1 发布验收；复审重点：五项冻结边界、与 02-00 的兼容映射、无第二权威来源——将按本协议输出追溯矩阵）。

**下次更新触发条件**：T-004 提交审查
