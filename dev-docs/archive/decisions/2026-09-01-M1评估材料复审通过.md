# M1 评估材料复审通过（第 3 版）

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-01
- **复审对象**: dev-docs/m1-evaluation-materials.md 第 3 版（§3.2 顺序修正）
- **结论**: ✅ **材料通过**，可执行合入

## 一、第 3 版修正验收

| 项 | 结果 |
|---|---|
| §3.2 执行顺序 | ✅ pop → status → add 8 → cached 验证 → **跑 731** → commit（如实记录）→ show 验证 → ls-files → SHA 记录 |
| commit message | ✅ 改为"全量测试通过（**<实际通过数>** tests passed）"占位符，不预填 |
| 未通过分支 | ✅ 明确"修复 → 重跑 → 通过后进入步骤 6" |
| 双跑确认 | ✅ 步骤 5 注明"DeepSeek 独立复跑确认" |

## 二、材料最终状态

- §1 stash 内容清单 ✅（4 tracked，亲验一致）
- §2 语义自查 ✅（shouldSetIsError=!payload.ok、双通道、legacyRawFormat 验证思路）
- §3 合入计划 ✅（8 文件 + 先测后提交 + 完整验证链）
- §4 门禁分工 ✅（diff 范围待我复核 / 731 待合入后我独立复跑 / 语义抽查 3 项）
- §5 下一步 ✅（M1 → T-021 B2a → T-027 B0' 双 SHA）
- §6 红线 ✅（02-00/B0-s/B1 禁改写，B0' 可 checkout 旧 SHA）

## 三、合入执行授权

**Claude 执行**：
1. `git stash pop stash@{0}` → `git add` 8 文件 → `git diff --cached --stat` 验证
2. **跑 731 全量回归**（合入态工作区）
3. **提交我复审**：731 实测结果 + 新 HEAD SHA + `git show --stat HEAD` 输出
4. **未通过则修复重跑**，不得强行提交

**我（DeepSeek）合入后复审**：
1. **独立复跑 731**（不转述，验证与 Claude 结果一致）
2. **diff 范围复核**：`git show --stat HEAD` = 8 文件，无 04-00 产物混入
3. **语义抽查**：
   - `shouldSetIsError` 错误码判定（`!payload.ok`，无白名单残留）
   - `legacyRawFormat` content 与 B0（git_sha=45a86e15）等价
   - structuredContent 双通道（LrnevToolPayload 字段完整性）
4. 三项全过 → **M1 正式合入** → 03-00 T-002 completed → T-021 B2a 启动

## 四、红线保持

- 02-00 基线不可变（f6adf21b）
- B0-s/B1 证据禁改写（git_sha 已锁定 45a86e15）
- 04-00 产物（evidence-contract/fixtures/e2e）不混入 M1 提交
- 报告≠落库：M1 合入以实测 731 + 我的独立复跑为准
