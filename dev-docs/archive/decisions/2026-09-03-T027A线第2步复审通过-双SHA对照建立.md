# T-027 A 线第 2 步复审通过：E-01 双 SHA 有效对照建立

- **日期**: 2026-09-03
- **作者**: DeepSeek（审查方）
- **复审对象**: 提交 `f331a3f`（frontmatter + 参数判定）、`a6b54a7`（服务端解析判定 + sha-b 证据）、`6914392`（无效标注）
- **复审方式**: sha-b 第 3 次会话 JSONL 逐调用核对 + frontmatter 产物验证 + 提交 diff 抽查
- **结论**: ✅ **通过**。P0 三项修复真实生效；**sha-b E-01 有效观测达成**（无 -32602、scene 参数正确、产物落位正确）。**E-01 双 SHA 有效对照建立**——T-027 B0' 行为基线的第一个可用场景对照。可进入 A 线第 3 步。

## sha-b 第 3 次运行亲验（e-01-1788419034219-05n45akm）

| 调用 | 输入 | tool_result 实况 |
|---|---|---|
| 1. project_status | {} | ✅ ok:true **无 -32602**（frontmatter 修复生效：created:"2026-09-03" string；场景 01-user-management 与 spec 01-00-introduction 可见） |
| 2. **spec_create** | **{name:user-login, scene:01-user-management, priority:P0}** | ✅ ok:true，spec=02-00-user-login，**scene=01-user-management**，path 落位正确 |
| 3. Read requirements.md | 02-00-user-login 路径 | ✅ 成功（产物真实存在；frontmatter `created: '2026-09-03'` 带引号——修复在产物侧同步生效） |

## 双 SHA E-01 有效对照（基于亲验）

| 维度 | sha-a (45a86e15) | sha-b (6383e99) |
|---|---|---|
| 工具序列 | project_status → spec_create → Read → Edit → Write（5 步） | project_status → spec_create → Read（3 步） |
| spec_create scene 参数 | `user-management`（name 格式） | `01-user-management`（完整 id） |
| 产物 | 02-00-user-login @ 01-user-management ✅ | 02-00-user-login @ 01-user-management ✅ |
| 附加行为 | Edit/Write 完善内容（权限拒） | 无（更简洁） |
| 核心动作 | ✅ spec_create 成功 | ✅ spec_create 成功 |

- 行为随机性观察（sha-b 三次：探索型/直接型/探索型）合理——印证 F-03 需 ≥5 次重复采样；
- 无效证据标注（nlsi4s2a-INVALID.md）✅ 保留历史；
- 参数判定逻辑（从 tool_result 提取服务端解析结果，兼容 name/id 格式）✅。

## 遗留（第 3 步一并处理）

1. **2 个 untracked jsonl**（e-01-1788417936931-d6tvmdq5 / e-01-1788418094442-93mu8prf——sha-b 中间运行录制，无 evidence json）——归档标注或删除；
2. Edit/Write 文件编辑权限（E-07 场景前置：限定工作区路径的允许列表）；
3. mcp_version 统一取协商值（2025-11-25）；
4. E-06b 分轮注入 + fixture 权威源接入（12 场景去硬编码，decisionContext 驱动工作区构建——**frontmatter 日期引号规则须内置到工作区构建器**，防止其他场景复现 -32602）。

## 下一步指示（A 线第 3 步）

1. **fixture 权威源接入**：harness 从 `tests/fixtures/04-00/*.ts` 加载 12 场景（去 E-01 硬编码）；工作区构建器按 decisionContext 生成（**frontmatter 引号规则内置**）；prompt 只读 userInput；
2. **多轮注入**：E-05/E-06a/E-06b 轮次结构支持（E-06b 监听 spec_create(B) 成功后注入第二轮）；
3. **参数级判定**：expectedAction + scene 等关键参数对照（基于 tool_result 服务端解析结果）；
4. **文件编辑权限**：E-07 需要（限定工作区路径）；
5. 12 场景 × sha-a 先各跑 1 次（冒烟级）验证场景前提（precheck 粒度断言 + 实际行为记录），报 DeepSeek 复审后放量 5 轮。
