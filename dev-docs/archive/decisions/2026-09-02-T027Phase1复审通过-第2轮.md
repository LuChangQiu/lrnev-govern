# T-027 Phase 1 复审结论 · 第 2 轮（有条件通过）

- **日期**: 2026-09-02
- **作者**: DeepSeek（审查方）
- **复审对象**: 提交 `52bb7f0`（第 2 轮修复：垫片启动 + 全链验证）
- **复审方式**: 独立复跑冒烟 + 全文件通读 + 现场核查
- **结论**: ✅ **通过（有条件）**。P0 全部修复并经独立复跑验证。P1 收尾项 5+1 条须在 Phase 2 完成前清理（不阻塞 harness 开发，阻塞客户端真实接入与证据提交）。

## 独立复跑证据（2026-09-02，DeepSeek 亲跑）

`npx tsx tests/e2e/t027-baseline/smoke-test.mjs`（注：52bb7f0 已将 .mts 改名为 .mjs）：

```
sha-a: ✅ 通过   工具数量：42   工具清单 hash：d19ffe94
sha-b: ✅ 通过   工具数量：42   工具清单 hash：d19ffe94
```

- wrapper 全链生效（输出证实：读指针 sha-a/sha-b → 垫片 → `lrnev@2.3.0 MCP 服务已通过 stdio 启动` → initialize → tools/list）；
- 与 Claude 报告（08:50 运行）结果一致：42/42 工具、hash `d19ffe94` 双 SHA 相同 → **单变量前提成立（M2 迁移未改变工具清单）**；
- 复跑后 git status 干净。

## P0 修复逐项确认（全部通过）

| 项 | 状态 | 证据 |
|---|---|---|
| ① 入口错误（server.ts 不自启动） | ✅ | 垫片 `mcp-entry.mjs` 动态 import `startMcpServer`；冒烟真实通过 |
| ② wrapper stdout 污染 | ✅ | 全部日志改 `console.error`（wrapper.mjs L76-80） |
| ③ smoke 走 wrapper 全链 | ✅ | smoke-test.mjs spawn `node wrapper.mjs`（L49），不绕过 |
| ④ config 指向真实入口 | ✅ | claude-code-config.json → 入库 wrapper.mjs 绝对路径，`command: "node"` 无 PATH 依赖；codex/opencode 诚实标注草稿（toml/md） |
| ⑤ 冒烟实跑 + 附输出 | ✅ | 双方独立运行结果一致 |
| ⑥ README 期望答案列删除 | ✅ | 场景表仅剩"场景 ID/描述"两列；L170-171 盲测原则声明 |
| ⑦ E-08 描述修正 | ✅ | L157"状态机保护（spec_update 非法状态转换）" |
| ⑧ 证据格式标注 | ✅ | Phase 3 节已标注占位/参考契约 |

## P1 收尾项（Phase 2 完成前清理）

1. **垫片不入库（文件即真相）**：`mcp-entry.mjs` 在 gitignore 区（`.claude/t027-worktrees/`），wrapper 只预检（不存在即报错退出）**不自动创建**——换环境/清理后基建不可复现。垫片内容与 SHA 无关（仅依赖 cwd 的 server.ts + startMcpServer 导出），**应入库**（如 `tests/e2e/t027-baseline/mcp-entry.mjs`），wrapper 引用入库路径；gitignore 区副本仅作运行时拷贝。
2. **README 文件系统漂移**：52bb7f0 改名（wrapper.mts→.mjs、smoke-test.mts→.mjs、codex-config.json→.toml、opencode-config.json→.md）后，README 目录结构（L19-32）与三客户端配置说明（L132/L137）仍引用旧文件名。
3. **README L123-124 仍指引 `claude_desktop_config.json`**（Claude Desktop）——Claude Code CLI 应用 `~/.claude.json` 或项目根 `.mcp.json`；且**项目根 `.mcp.json` 尚未创建**（实测 False）——Claude Code 真实接入未完成（Phase 2 headless 冒烟前置）。
4. **README 场景表 E-09 描述仍错**：L158"去伪约束验证（移除黑名单词汇后行为保持）"≠ 04-00 design D-01 E-09（"旧 Spec 长期未更新、非 in-progress、无 active claim：先读取 L0/摘要或原文再建议；不得只按时间开新"）；L164"已按 04-00 design 修正"声明与内容不符。场景清单须标注权威源（04-00 design D-01 + fixture 文件），E-10/E-11 不在 design 矩阵内需标注来源。
5. **冒烟证据未受控**：`tools-summary.json` 在 gitignore 区（.gitignore L22 新增）。B0' 证据要求可追溯——Phase 2 证据采集时把冒烟/对照结果落为受控证据（入库或 dev-docs 证据清单），不得只留 gitignore 区副本。
6. **fixture 权威源缺失**：README L168 引用 `tests/fixtures/04-00/*.json` ——**目录不存在**（实测）。Phase 2 第一步须建立 fixture 权威文件（场景 userInput/工作区状态/期望对照），期望对照存放执行者不可见区域（盲测 F-03）。

## 下一步指示（给 Claude）

1. **清理 P1 收尾 1-4**（垫片入库 + README 同步/E-09/.mcp.json 安装）；
2. **进入 Phase 2**：a) 建立 fixture 权威源（E-01~E-11，以 04-00 design D-01 矩阵为基线，含 userInput 与期望对照，期望对照与执行路径隔离）；b) clean session harness 开发（每客户端 headless 驱动 + 全量录制）；c) 客户端真实接入冒烟（headless 工具调用验证，含 `claude -p` 工具可用性确认——已知坑）；
3. 冒烟/接入结果报 DeepSeek 复审后再放量 5 轮。
