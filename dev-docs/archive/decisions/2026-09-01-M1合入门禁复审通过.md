# M1 合入门禁复审通过（第 3 轮）

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-01
- **复审对象**: M1 合入最终状态（HEAD=131e6f3）
- **结论**: ✅ **三项门禁全过，M1 正式合入**

## 一、门禁验收汇总

### 门禁 1（diff 范围）✅
- M1 提交 3c30710：8 files +1397/-100（tracked 4 + untracked 4），无 04-00 产物混入
- .gitignore +4 行（无重复、无 /dev-docs/），registry.json 无运行态
- 证据入库 bc11a11：6 files +15233（5 证据 + 材料），git_sha 字段保持 45a86e15
- 证据已 tracked（git ls-files 确认），stash@{0} 已清

### 门禁 2（731 实测）✅
- **我独立复跑（HEAD=131e6f3）：764/764 passed（60 files，19.19s，exit=0）**
- e04/e07 断言已修正：硬编码 SHA → `/^[0-9a-f]{40}$/` 格式验证（commit 131e6f3）
- Claude 已诚实说明此前"764"是暂存区状态（HEAD 仍 45a86e15）跑的——时间绑定断言缺陷 + 测试时机错误，已修复

### 门禁 3（语义抽查）✅
| 抽查项 | 结果 |
|---|---|
| shouldSetIsError | ✅ `return !payload.ok`，无白名单残留（AMBIGUOUS_REF 等错误码不再豁免） |
| LrnevToolPayload 结构 | ✅ response_version='1' + 索引签名（MCP SDK 兼容）+ 注释明确 D-05 isError 映射 |
| legacyRawFormat B0 等价 | ✅ 只读工具 content 直出 `JSON.stringify(data)`；有 ai_followup/errors 时用完整 envelope（信息不丢） |
| structuredContent 双通道 | ✅ content（legacy）+ structuredContent（payload）+ isError 三路齐全 |

## 二、M1 合入正式确认

- **HEAD**: 131e6f339d3e1665130ee46ec36bf50de481cc85
- 提交链：45a86e1（B0 基线）→ 3c30710（M1）→ bc11a11（证据入库）→ 131e6f3（断言修正）
- 731/764 全过、证据可版本审计、语义正确

## 三、合入后指令

1. **03-00 T-002 → task_update completed**（reason：`M1 合入 131e6f3，764/764 实测通过（DeepSeek 独立复跑确认），门禁三项全过`）
2. **T-021 B2a 启动**（M1 双通道后测试套件）：
   - 复跑 04-00 套件（B2a 阶段：structuredContent 引入后模型可见信息保持 B1）
   - 产出 B2a 证据（--stage=B2a）
   - B2a vs B1 对照（核心 surfaces 消费率 ±5% 等价判定，T-025 口径）
3. **stash@{1} 备份清理**：确认 M1 稳定后 drop（防误用旧版本）

## 四、红线保持

- 02-00 基线不可变（f6adf21b，未动）
- B0-s/B1 证据已入库（bc11a11），git_sha 字段 45a86e15 不变
- 报告≠落库：M1 合入以实测 764 + 独立复跑为准（已完成）
