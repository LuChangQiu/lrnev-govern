# 文件治理汇总裁决表（四份审计 + 执行批次，DeepSeek 汇总）

- **日期**: 2026-09-07
- **审计**: 产品面（e283af2a）/ tests 面（7f9a86b4）/ dev-docs 面（7c53b580）/ .lrnev 与杂项面（3f5d6381）
- **状态**: 待用户拍板执行

## 一、总判定

**763/776 tracked 文件归属正确、路径合理**。需治理动作集中在四块 + 一个重要事实修正。

### ⚠️ 重要事实：GitHub 端实际滞后 159 commits（未 push）

分支 ahead origin/main **159 个未 push 提交**——GitHub 上仓库停在早期状态。**用户问"发布到 GitHub 是否都保留"的实质 = push 后才会有这些内容**。文件治理应先于首次大 push 完成（一次推干净的结构）。

## 二、四份审计的裁决建议

### A. .lrnev 半跟踪（3f5d6381 + e283af2a，方向冲突已裁决）
- 实测 67 tracked + 42 untracked（全忽略）——半跟踪事故（仅 registry.json 是故意出库）。
- **悬空引用铁证**：dev-docs 内 **129 处** `.lrnev/scenes/04-*` 引用 + t027 README 引用 + T-005 跨机 hash 设计前提 = scene04 文件可 checkout 复现 → **scene04 不入库则 GitHub 证据全悬空**。
- **裁决（支持 C5 已拍方向 + 方案 B）**：治理文档入库，运行态精确忽略：
  1. .gitignore 删 `/.lrnev/` 整忽略 → 精确忽略：`.lrnev/agents/` `.lrnev/runtime/` `.lrnev/locks/` `.lrnev/config/` `.lrnev/**/*.backup` 等；
  2. **补入库 31 文件**：08-00 d/r ×2（已被入库证据 JSON 引用——悬空实锤）、09-00 整 spec ×3（暂缓判定记录）、scene04 canonical 26（scene/arch/roadmap + ADR + specs 五套三文档；**ADR 格式与库内约定漂移需先确认**；04-00 的 4 个工作残留排除）；
  3. **删空壳**：00-default/03-00-test-spec 全 FILL 骨架 ×3；
  4. scene04 与 dev-docs 分工声明（.lrnev=治理运行面/验收真值，dev-docs=研究档案面，补一句消歧）。

### B. dev-docs 收敛（7c53b580）——留 GitHub，三批收敛 2.17MB→~1.3MB
- **批 1（机械零联动）**：删 src 快照副本 + inventory v1 代（~473KB）；06-00 两份（client-integration-guide/mcp-response-conformance——spec 明言"无条件发布"的用户文档）**迁 docs/**；归档 11 份（m1 材料、04-00 五阶段对比、T-002×2、semantic-violations/cases、baseline-report、execution-constraints、b0/b1 报告、REVIEW-BRIEF）→ archive/。
- **批 2（1 处联动）**：baseline-report 归档需同步 scan-high-risk-wording.ts 输出路径；中文文件名统一 kebab（t027 README 1 行联动）。
- **批 3（拍板）**：inventory 留 v2+v3-08-00+v3-runtime 删 v1（已含批 1）；06-00-e2e-evidence-index 随迁或留；嵌套 ai-discussions 镜像移本机 or archive（倾向移本机）；顶层 3 活文档刷新到 3.0.0。

### C. 产品面小修（e283af2a）
1. scripts/semantic-analysis-output.json（非合法 JSON、0 引用）→ git rm --cached；
2. 4 个零引用一次性脚本（b0-pre-check.sh/b0-post-restore.sh/fix-b1-invertibility.mts/generate-inventory-markdown-v2.ts）→ 移 dev-docs/archive/（注：b0-pre/post 被 .lrnev runtime 文件引用为读写方——归档后 runtime 指针文件仍忽略，无碍）；
3. README:219 删 ai-discussions 表述（GitHub 不存在该目录）；.gitignore #9f 注释引用裁决文档断链 → 改指 dev-docs 镜像或注明本机；
4. t027 两工具（batch-runner/f04-stats）在 t027-baseline README 登记调用方式；
5. 版本三源漂移（package.json 2.3.0 / lock 2.2.0 / README 3.0.0）→ 发布动作一次对齐（P0）。

### D. tests 面（7f9a86b4）——零动作
全部归属正确；t027-baseline 承重固化保持原位；录制 100% 隔离。4 项低危观察记录（current-sha 文档过期、配置含本机路径、冒烟报告分布、fixtures 双套命名）。

## 三、执行批次

| 批 | 内容 | 时机 |
|---|---|---|
| **P0 发布对齐** | npm version 3.0.0 + npm i（lock 2.2.0→3.0.0）+ CHANGELOG [3.0.0]（草稿已审） | 用户确认后立即 |
| **P1 文件治理** | A（.lrnev 方案 B：.gitignore 重构 + 补入库 31 + 删空壳 3）+ B 批 1/2（dev-docs 收敛）+ C1-C4（产品面小修） | 用户拍板后执行（一个整理 commit 或分几个） |
| **P2 GitHub 发布** | git push（159 commits 首次全量上 GitHub）→ 验证 → npm publish | 用户确认后（tag v3.0.0） |
| **P3 发布后批** | dev-docs 批 3 拍板项、顶层活文档刷新、tests/代码重构、.lrnev scene04 ADR 格式确认 | 发布后 1-2 周 |

## 四、待用户拍板点

1. **P0**：执行 3.0.0 发布对齐（CHANGELOG + bump + lock）？
2. **P1**：文件治理批（.lrnev 入库方向 + dev-docs 收敛 + 产品面小修）——按 A/B/C 执行？
3. **P2**：push 到 GitHub（159 commits 首次全量）——push 前或后跑 npm publish？（建议 push 后 tag v3.0.0 再 publish）
4. **B 批 3 项**：inventory 留代方案 / 06-00-e2e-evidence-index / 嵌套镜像处置 / 顶层活文档刷新（可默认采纳建议）
