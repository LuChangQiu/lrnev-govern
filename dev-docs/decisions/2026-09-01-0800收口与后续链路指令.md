# 08-00 收口与后续链路指令（DeepSeek 审查方发布）

- **发布人**: DeepSeek（审查方 / 全局协调）
- **日期**: 2026-09-01
- **状态**: 08-00 运行时迁移完成（dc49820 + c79c322），进入 P0 后续链

## 一、收口确认

| 项 | 状态 |
|---|---|
| 08-00 迁移提交 | ✅ dc49820（10 文件，+355/-9） |
| T-001~T-005 落库 | ✅ c79c322 全部 completed（亲验 tasks.md） |
| 773/773 实测 | ✅（我独立复跑确认） |
| T-028 0 high | ✅（迁移后重跑） |
| 授权偏离落档 | ✅ tasks.md L41-44 |

## 二、后续链路（P0 剩余 + 并行项）

### 链路 1：B1-运行时证据（08-00 迁移的闭环验证）
1. **更新 v3 标注文件**：迁移后新文本（guidance.ts 已变）的 346 surface 重新标注 → 新文件（如 v3-08-00-migrated.json 或就地更新 + 版本记录）
2. **采集 B1-运行时证据**：`npx tsx scripts/run-b0-baseline.mts --stage=B1 --baseline-ref="08-00 运行时迁移后"`——此时 content_hash 基于**新文本**，与旧 B1-标注证据（v3 旧文本 hash）**应不同**，这正是迁移生效的证明
3. **对照**：B1-运行时 vs B1-标注（迁移前后文本 hash 差异）+ 行为字段 12/12 保持（迁移不改判定逻辑）
4. 提交复审 → 通过后 B1 阶段真正闭环

### 链路 2：T-027 B0' 双 SHA（真实客户端，前置已齐）
- SHA A = 45a86e15（02-00 运行时文本）
- SHA B = dc49820（08-00 迁移后运行时文本）——**运行时文本真实不同**，双 SHA 对照现在有意义了
- 前置：真实客户端接入基建（Claude Code 主 + Codex 补，≥5 clean sessions）
- acceptance 按已扩写的 T-027（盲测原则 + F-04 门禁 + 双 SHA 行为快照）

### 链路 3：M2/B2b（可并行）
- M2 = 03-00 content 切 ModelVisibleContract（纯服务端）
- 完成后 T-023~T-024（B2b 测试 + 证据）→ T-025 综合对比

## 三、执行顺序建议

```
1. 更新 v3 标注 + B1-运行时证据采集（链路 1）——先做，08-00 闭环
2. M2 开发（链路 3）——可并行
3. 真实客户端接入基建（链路 2 前置）——可并行启动
4. T-027 双 SHA 对照（链路 1+2 齐后）
5. B2b（M2 后）→ T-025/T-026 综合报告
```

## 四、红线保持

- 02-00 基线不可变、B0-s/B1-标注/B2a 证据禁改写（已入库）
- 08-00 迁移不改执行逻辑（已确认）
- 新 v3 标注文件：迁移后新文本的标注，与旧 v3（02-00 文本标注）区分存放，不覆盖
- 报告≠落库（验收以实测 + 独立复跑为准）
