# T-027 B3 对照汇总：sha-c（修复后快照）真机重测

- **日期**: 2026-09-04
- **作者**: DeepSeek（审查方）
- **快照**: sha-c = 918581e（G1-G4 + 05-00 Profile + 输出契约根治 a586f46 + 归一判定）
- **批次**: claude E-02/E-06a/E-06b ×5（15 sessions，$5.23）+ opencode E-02 ×5（最终批，$0.05）
- **证据**: tests/e2e/t027-baseline/.evidences/（全部 validator strict 0 ERROR、git_sha=918581e、sha_label=sha-c），提交 ec78bce/7ee9c28

## 一、判定矩阵（PASS/5，三 SHA 对照）

| 场景 | 客户端 | sha-a | sha-b | **sha-c** | 变化 |
|---|---|---|---|---|---|
| E-02 | claude | 0/5 | 0/5 | **0/5** | 无（no-reg 稳定） |
| E-02 | opencode | 5/5 | 2/5 | **4/5** | sha-b→sha-c：-32602 artifact 归零；1 FAIL = 模型未行动（真实行为） |
| E-06a | claude | 1/5 | 2/5 | **1/5** | 无（自动归档仍 4/5——G5 不在快照，预期内） |
| E-06b | claude | 3/5 | 3/5 | ~~5/5~~ 伪PASS‡ | ‡勘误：split 驱动 artifact（模型零动作），B4 真实续接重测 |
| E-01 | opencode | 5/5 | 5/5* | — | *终判（918581e 归一复核翻盘） |
| E-07/E-08 | 全 | 5/5 | 5/5 | — | 稳定面（未重测，无代码路径变化） |

## 二、核心结论

1. **输出契约修复验收通过（发现 #3 闭环）**：opencode sha-c 全程 **0 个 -32602**（sha-b 33 错归零）。E-02 从 sha-b 2/5 → sha-c 4/5，残余 FAIL 为真实模型行为（1 次只读调研未行动）。B3 起 opencode 行为首次无污染可见。
2. **G1-G4 对 claude E-02 无效（激励缺口实证，发现 #1 部分闭环）**：E-02 claude 三 SHA 稳定 0/5（从不 task_create，直接 Edit）。抽查 session 原始记录：spec_get 的 tool_result **明确含 G1 引导**【建议】已有 Spec 下新增可独立验收的开发增量 → 用 task_create 登记任务——模型读到后仍直写。**认知缺口已修（引导送达），行为不变 → 激励/路径成本问题**：单会话最短路径（直写）vs 跨会话登记价值。且引导措辞"仅修改既有正文内容 → 可直接编辑"可能给直写提供了合法归类（模型把"补充登录"理解为补 requirements 正文）。→ **L6 结构性杠杆**（设计裁决文档）需用户拍板。
3. **E-06a vs E-06b 行为分化——勘误（2026-09-04 晚，session 级分析 5cf57d9d）**：~~E-06b 5/5 = AI 尊重"用户明确要求的已建成物"、E-06a 4/5 = 撤销"自己抢跑的产物"（所有权感知假说）~~——**该归因不成立**。两场景 harness 驱动不对称：E-06a round2 是 `--resume` 真实续接（模型是刚建 B 的当事人，收到第一人称"等等先别建了"）；E-06b round2 是**全新会话注入叙述式场景全文**（含"第2轮（AI 已执行 spec_create）"舞台指示）→ E-06b 5/5 模型把输入判为"假设场景测试"，回澄清提问、**零工具调用**——B 保持 draft 只是因为什么都没做（5/5 未做 task_create(A)，iaraomst/njlg5yoo 甚至明文把归档 B 写为正确做法）。**E-06b 5/5 为"伪 PASS"（驱动 artifact），sha-a/b 的 3/5 亦同驱动下结果**。真正差异 = 会话连续性/当事人身份 + 话术撤销指向。归档决策链（E-06a FAIL 4/4 同构）：同一条 thinking 内"用户不要独立 Spec → B 是我刚建的 → 无删除工具 → spec_update archived"，无 spec_get 复核、无征询。详见 ai-discussions/结果/2026-09-04-DeepSeek-E06ab分化Session分析.md。**B4 修正：E-06b 改真实续接双轮 + 判定加 task_create(A) 正动作门**。
4. **判定口径修订（本 B3 轮次）**：E-02 单条 task_create 的 title 全等是**实现与 v2 裁决不一致的 bug**（裁决 = 命中目标 A（scene/spec）+ 成功登记；title 是 AI 拟题自由——用户原话从不指定标题）。修正 9959688 后 opencode 批 2 例 title 措辞差异不再误判（修正直接证据）。E-01 归一终判（918581e）：opencode sha-b 2/5 → 5/5（字面比较 artifact）。
5. **E-06b sha-c 5/5 不作改善结论（勘误）**：原"sha-c 5/5 vs sha-a/b 3/5 改善"表述撤回——5/5 是分轮注入的伪 PASS（模型零动作，见结论 3 勘误）；sha-a/b/c 的 E-06b 全为 split 驱动下结果，横向可比但都不测"是否尊重已建 B"。E-06b 真实行为需 B4（真实续接驱动）重测。

## 三、成本

B3 总计 ≈ **$5.28**（claude $5.23 + opencode $0.05）。T-027 全程累计（含探索/重跑/终判）≈ $48.8（$50 闸内，最终核算以 batch 汇总为准）。

## 四、下一步

1. **G5 进快照 → B4 验证 E-06a**（G5 对"抢跑撤销"语境是否有效）——待用户裁决 L6 方向后一并设计（B4 可与 L6 实验合并）。
2. **L6 结构性激励设计**（E-02 claude 0/5 实证）——候选 ① project_status/报告对"有改动无登记"债务信号；② gate/状态机与登记的联动强化；③ 接受 claude 直写为合法模式（design 语义修订）。**需用户拍板**。
3. **05-00 T-006**（B3 证据已足）：Profile 字段保留/回退裁决 + 适配结论落库。
4. 统一审查剩余：#6 类型门禁（执行中）、#9f 证据入库策略（调研中）、#2/5/7/8 核验（执行中）。
5. CHANGELOG/发布——**停下与用户讨论**（硬性门禁）。
