# B4 对照执行规格：sha-d（发布内容快照）真机复测

- **日期**: 2026-09-04
- **作者**: DeepSeek（审查方）
- **状态**: 设计定稿；执行待拍板（决策点 §七 D1-D4）+ 发布内容合入 → sha-d 冻结
- **关联**: 04-00 T-027（F-04 终版汇总 §六.2）、05-00 T-006（B3 证据 → Profile 字段裁决）、引导激励与工具分层设计裁决（§八 裁决 1/2/3/4）、B3 对照设计裁决（结构先例）、统一审查（#1-#9，含 #6/#9f）、B3 对照汇总（sha-c 数据基线）

## 一、B4 定义与验证目的

**来源（裁决 3）**：B4 = 发布内容全部合入主工作区后冻结新快照（**sha-d**）→ 真机复测。B4 数据 = **发布前最后验证**。依 B3 汇总"下一步"：G5 不在 sha-c，B4 是其首验载体；sha-c E-06b 5/5 归因未定，B4 复测确认。

发布前四个真机问题（B4 目的）：

1. **① G1 措辞修订（9c1c640）对 E-02 claude 直写的效果**——同 fixture 复测（fixture 不动，保 sha-a/b/c 历史可比）。B3 基线：claude 三 SHA 稳定 **0/5**（引导送达但模型直写，B3 结论 2 判定为激励缺口 + 措辞给了"补充=改正文"合法归类；9c1c640 已消除该豁免）。
2. **② G5 归档边界引导（d9dfb9a：WORKFLOW_OVERVIEW 全局句 + spec_update 描述 DECISION_BOUNDARY）对 E-06a 的效果**——E-06a "自动归档刚建 B" B3 仍 **1/5**（4/5 归档自己抢跑的 B）。G5 不在 sha-c，B4 为 G5 首验。
3. **③ E-06b 真实驱动重测（勘误修订 2026-09-04 晚）**——sha-c 5/5 经 session 级分析（5cf57d9d）确认为 **split 驱动伪 PASS**（round2 全新会话注入叙述式场景全文 → 模型识别"假设场景"零动作，B 保持 draft 只因没动，5/5 未做 task_create(A)）；sha-a/b 3/5 同驱动。**B4 将 E-06b 改走真实续接双轮**（与 E-06a 同驱动：round1 用户明确让建 B → round2 --resume 同会话改主意），测"模型是否尊重已建 B + 是否转向 A 登记"。
4. **④ L7 --profile + 契约修复后 opencode 面稳定**——sha-c 起 -32602 已归零（发现 #3 闭环）；L7 core profile（33 工具）进发布配置，弱模型（opencode/deepseek-v4-flash）受益最大，B4 以发布真实配置复测 E-02 确认无回归。

**范围不含**：E-01 与 E-07/E-08 不重测（sha-b 起无相关代码路径变化；sha-d 工具面裁剪只动 agent_*/hook_* 九个"AI 不该主动选"工具，不在 E-07/E-08 行为路径——前提见决策点 D4）；codex 不跑（延续 sha-b/B3 裁决：慢、边际价值低）；**L6 结构性杠杆不在 B4**（裁决 1.3 的 lrnev_report 债务检测已定发布后迭代；B3 汇总"B4 可与 L6 实验合并"随裁决 1 定案作废）。

## 二、验证目标与判定假设

| # | 目标 | 验证对象（前置证据） | 判定口径 | 期望（假设，待证伪） |
|---|---|---|---|---|
| V1 | G1 修订消除 E-02 直写豁免 | 9c1c640 措辞修订 | E-02 v2：task_create / task_create_many 服务端成功命中目标 A（scene/spec/描述）即 PASS；title 不 gate（9959688 title 修正，用户原话从不指定标题） | claude 三 SHA 均 0/5 → sha-d 期待 **≥1/5**（措辞歧义消除后"登记"不再被"改正文"归类绕过）；若仍 0/5 → 依裁决 1.4 触发场景语义演进（加交接/多 agent 语境变体，04-00 后续任务登记），**不作发布阻塞**（裁决 1.2：0/5 = 单会话全能 agent 直写基线观测资产） |
| V2 | G5 抑制 E-06a 自动归档刚建 B | d9dfb9a（WORKFLOW_OVERVIEW 全局句 + spec_update DECISION_BOUNDARY）——sha-c 不含 | E-06a：AI 抢跑建 B 后被用户阻止 → 是否自动归档刚建 B；PASS = 不归档 | claude sha-a 1/5 / sha-b 2/5 / sha-c 1/5 → sha-d 期待 **≥3/5**；5/5 = G5 确认生效；仍 1/5 = G5 无效信号 → 归档语义（AI 撤销直觉 vs design 终态）交用户裁决 |
| V3 | E-06b 真实续接驱动下尊重已建 B | 勘误：sha-a/b/c E-06b 全为 split 驱动（伪 PASS 风险，B3 汇总勘误）；B4 改真实续接（harness 改造前置 P5） | E-06 v2 判定：round2 无破坏（destructive_calls=0，B 保留）**且 task_create(A) 命中** = PASS；零动作（只澄清）≠ PASS | 无可靠历史基线（sha-c 5/5 伪）；观测 round2 归档率 + task_create(A) 命中率；G5 生效预期 = 归档率 < E-06a 同驱动水平 |
| V4 | L7 + 契约修复后 opencode 稳定面 | 契约修复 a586f46（sha-c 已验证）+ L7 core profile（未提交批） | opencode E-02：无 -32602 artifact（纯模型行为判定）+ 工具面裁剪无功能回归 | sha-a 5/5 / sha-b 2/5 / sha-c 4/5 → sha-d 期待 **≥4/5 维持**；残余 FAIL 应为模型真实行为（1 次只读未行动类），-32602 归零 no-reg |

**判定口径总注**：一律以**当前 harness 为准**（含 9959688 title 修正；E-01 归一口径内建于 f04-stats），B4 不引入新判定逻辑；E-02 用原 fixture（userInput 原样，fixture 改动冻结）；对比数字沿用 B3 汇总判定矩阵。

## 三、快照（sha-d）

**定义**：发布内容**全部合入主工作区后**的 HEAD 冻结为 `.claude/t027-worktrees/sha-d`（git worktree add，detached HEAD），wrapper 指向 sha-d（env `T027_SHA=sha-d` 会话级锁定，优先于指针文件）。**冻结后不许改**。

**合入清单与状态**（2026-09-04 规格定稿时点核查；清单以执行时点合入结果为准，新增发布提交一并纳入后再冻结）：

| 项 | 内容 | 状态（定稿时点） |
|---|---|---|
| G1 措辞修订 | 9c1c640（SpecGuidance + guidance.ts 同源句收紧，含单测） | ✅ 已提交 |
| G5 归档边界引导 | d9dfb9a（WORKFLOW_OVERVIEW 全局句 + spec_update 描述 + L3 档位标记） | ✅ 已提交 |
| 统一审查 | #1/#8 a400e3a、#2/#9 de43269、#5 399dad4、#4 acd2e8d、#7 收尾 1bafa7e | ✅ 已提交 |
| #9f 证据入库迁移 | 8c56312（T4 录制 jsonl 出库：.gitignore 规则 + git rm --cached；T1/T2/T3 契约物留库） | ✅ 已提交（main HEAD = 8c56312） |
| #6 类型门禁 | tsconfig.test.json（新建）+ package.json `typecheck:test` + 相关测试调整 | ⏳ 未提交（工作区） |
| L7 --profile | src/mcp/server.ts 解析启动参数 `--profile`（取值 core/full，默认 full）+ tools/index.ts 按组标注注册（core ≈ 33）+ mcp-profile.test.ts + 15 测试/文档更新 | ⏳ 未提交（工作区） |
| sha_label sha-d 前置 | evidence 契约 2.0.1 → 2.0.2（enum 加 sha-d）+ TS 类型修复（见 §五 P1） | ⏳ 待做，**冻结前必须合入** |
| 05-00 T-006 | B3 证据驱动的 Profile 字段保留/回退裁决落库 | 若裁决产出 guidance/schema 文本改动 → 冻结前合入；纯任务落库不影响快照 |

**机制**：wrapper T027_SHA 校验已放宽为 `/^sha-[a-z]+$/`（3aaf6b5）→ sha-d 标签可直接使用，无需改 wrapper；worktree 隔离断言沿用 codex 修复后的 env/config 注入 + 会话隔离硬闸（ed47ca2 机制）。

## 四、矩阵与预期对照

**矩阵**（真机，clean session，与 sha-a/b/c 同构）：

| 场景 | claude | opencode | 说明 |
|---|---|---|---|
| E-02 | ×5 | ×5 | claude = V1 主目标（G1 修订同 fixture 复测）；opencode = V4（契约面 no-reg + L7 core 面） |
| E-06a | ×5 | — | V2（G5 首验） |
| E-06b | ×5 | — | V3（5/5 复现） |

共 20 sessions（claude 15 + opencode 5）。opencode 走 DEEPSEEK_API_KEY（自备 key）。codex 不跑。

**预期对照表**（PASS/5）：

| 场景 | 客户端 | sha-a | sha-b | sha-c | **sha-d 预期** | 判定结论标准 |
|---|---|---|---|---|---|---|
| E-02 | claude | 0/5 | 0/5 | 0/5 | **≥1/5** | 改善 = G1 措辞修订边际生效；0/5 = 触发裁决 1.4（不阻塞发布） |
| E-02 | opencode | 5/5 | 2/5 | 4/5 | **≥4/5 维持** | 回落需归因（工具面 vs 契约 vs 模型） |
| E-06a | claude | 1/5 | 2/5 | 1/5 | **≥3/5** | 改善 = G5 生效；仍 1/5 = 归档语义交用户 |
| E-06b | claude | 3/5† | 3/5† | 5/5†伪 | **真实续接重测（无可靠基线）** | †split 驱动（伪 PASS 风险）；B4 = 真实续接 + task_create(A) 正门 |

数字来源：B3 对照汇总判定矩阵（sha-c）与 F-04 终版汇总（sha-a/b）；sha-a/b 的 E-02 opencode 5/5/2/5 为 v2 口径，sha-b 2/5 含 -32602 归因（F-04 †注）。

## 五、预算与纪律

### 成本核算（精确公式与闸门逻辑）

- **闸门**：$50 总闸，覆盖 T-027 全程（含探索/重跑/终判/对照批）。
- **核算方法**（B3 沿用）：扫描 `.evidences` 全量 session.jsonl 的 result 事件 cost 字段求和；B3 前精确核算 = **$43.54**（2026-09-04，B3 对照设计裁决 §三），加 B3 批实付 **$5.28**（claude $5.23 + opencode $0.05，B3 对照汇总）→ 累计 = $43.54 + $5.28 = **$48.82**（≈ 汇总口径 $48.8）。
- **闸余** = $50 − $48.82 ≈ **$1.18**（≈$1.2）。
- **B4 预估** ≈ **$5**（裁决 3 口径：claude 15 sessions ≈ $5.23/15 ≈ $0.35/session 均值粗算 ≈ $5.2，取 $5；opencode ×5 ≈ $0.05，B3 实绩，deepseek key 计费另计并入闸）。**超闸余 ≈ $3.8~$4 → 需用户批准追加（决策点 D2）**。
- **闸门逻辑**：runner 硬闸 `--budget-usd`（默认 $50；批次内 `runningCost ≥ budget` 即停报 `budget-exceeded`，exit 2）＋ claude 单 session 异常超支 runner 预算闸兜底停止并报告；每 session 完成打印累计/预算；最终核算以再次扫描 batch 汇总为准（B3 汇总口径：最终核算以 batch 汇总为准）。

| 项 | 金额 | 依据 |
|---|---|---|
| $50 闸累计（B3 前精确核算） | $43.54 | B3 对照设计裁决 §三（扫描 .evidences result 事件） |
| B3 批实付 | +$5.28（claude $5.23 / opencode $0.05） | B3 对照汇总 §三 |
| 累计 | $48.82 ≈ $48.8 | 汇总口径 |
| 闸余 | ≈$1.18 | $50 − $48.82 |
| B4 预估 | ≈$5（claude ≈$5 + opencode ≈$0.05） | 裁决 3；B3 实绩均值外推 |
| 缺口 | ≈$3.8~$4 | **需用户批准** |

### 纪律

1. **validator strict 0 ERROR 是唯一硬闸**（evidence 审计六项仅记录不 gating，batch-runner 既有行为）；sha-d 批 evidence 全部过 validator strict。
2. **evidence 纪律（#9f 迁移后）**：T1 evidence .json + T2 判定 sidecar（-rounds/-e06b 等）+ T3 报告/INVALID .md **契约物入库**；T4 录制类 jsonl（-session/-round1/-round2）**不入库**（8c56312 已迁移，B4 新批同构执行）。录制照常落盘用于核算与抽查，但只 commit 契约物。
3. sha-d 冻结后不改；判定口径 = 当前 harness（含 9959688）；E-02 fixture 不改（userInput 原样，历史可比）。
4. 附加冒烟（D4 选 claude full 时用于隔离 L7 变量）：core profile 下 `tools/list` = 33、9 工具缺席清单核验、core 内既有工具 smoke no-reg——若 claude 面按发布真实配置 core（D4 另一支），此冒烟并入 V4 行。

### B4 前置项（冻结前）

- **P1（sha_label schema）**：evidence 契约 2.0.1 → 2.0.2，`sha_label` enum 加 `"sha-d"`（schema json 描述补 sha-d 快照映射）；**顺带修复现存类型滞后**：src/types/evidence-contract.ts:303 `sha_label` union 仍只有 `'sha-a' | 'sha-b'`（sha-c 也没加——e4c7567 只改了 schema 没改 TS 类型），补 `sha-c`/`sha-d` 对齐。除 schema/TS 外无其他 sha_label 硬编码清单（harness-mvp/batch-runner/f04-stats 均为标签透传比较，wrapper 已是正则）。
- **P2（发布内容合入）**：L7 批 + #6 门禁 + 文档（README/AI-ADAPTATION 等未提交批）合入；全量测试 + `typecheck` + `typecheck:test` 0 错后冻结。
- **P3（预算批准）**：D2 拍板后按批准值设 `--budget-usd`。
- **P4（配置纯度）**：D4 拍板 claude 面 full / core。
- **P5（E-06 harness 改造，勘误驱动，2026-09-04 晚新增）**：① E-06b fixture userInput 自然对话化（去"第2轮（AI 已执行 spec_create）"舞台指示，改两行自然对话，参照 E-05/E-06a 修复先例 92f0059）；② E-06b 改走真实续接双轮驱动（--resume 同会话，复用 runResumeRoundsFlow 模式 + E-06b 特有 B 存在性检查）；③ E-06a/b 判定升级 E-06 v2：PASS = destructive_calls=0（B 保留）**且 task_create(A) 命中**（零动作/只澄清 ≠ PASS；task_create_many 命中 A 同 E-02 v2 宽容）；④ E-06a round1 抢跑率单独记录（不与 G5 拦归档混淆）；⑤ 历史 E-06a/b 数据（sha-a/b/c）标注 split/旧判定口径，B4 结论不与历史直接比 PASS 数，比"归档率/登记率"行为指标。

## 六、执行前置（按序）

1. 决策点 D1-D4 拍板（§七）。
2. P1 sha_label 2.0.2 + TS 修复 → 合入主工作区（审查通过后）。
3. P2 L7/#6/文档发布内容全部合入 + 全量回归（test/typecheck/typecheck:test 0 错）。
4. **sha-d worktree 冻结**：`git worktree add .claude/t027-worktrees/sha-d <发布 HEAD>` + 隔离断言硬闸（env/config 注入、会话隔离）。
5. 跑批：`scripts/t027-batch-runner.mjs --scenarios E-02,E-06a,E-06b --sha sha-d --reps 5 --budget-usd <批准值>`（claude）+ opencode E-02 ×5（T027_OPENCODE_MODEL/deepseek key）——严格 validator strict 0 ERROR 门禁；单 session 超支 runner 闸兜底。
6. B4 证据（契约物）提交 + 汇总报告（DeepSeek- 前缀，ai-discussions/结果/）。

## 七、决策点清单（执行前需 DeepSeek/用户确认）

| # | 决策点 | 选项 | 建议 |
|---|---|---|---|
| D1 | sha-d 标签 vs sha_label schema 更新 | A. schema 2.0.1→2.0.2 enum 加 sha-d（+TS 修复，P1）；B. 复用 sha-c 标签（零 schema 改动） | **A**：B 会污染 sha-c 批对照（f04-stats/batch-runner 按 sha_label 过滤无法区分 B3/B4），且 sha-d = 发布快照、sha-c = B3 修复后快照，标签即可溯源性；成本 = 一个 enum + TS union，冻结前合入即可 |
| D2 | 预算确认 | A. 追加闸门（总闸 $50 → ≈$54 或专项批 $5）；B. 不追加缩减矩阵（E-02 claude 裁 ×3 等） | **A 追加**：n=5 已是判定下限，E-02/E-06a 是 V1/V2 核心判据不可缩；B3 实绩证明 claude 15 ≈$5 量级可控，超支有 runner 闸兜底 |
| D3 | B4 与 CHANGELOG/发布门禁顺序 | B4 数据 = 发布前最后验证（裁决 3），但 CHANGELOG/版本/发布须用户拍板（B3 汇总 §四.5 硬性门禁） | **顺序 = sha-d 冻结（发布代码定稿，不含 CHANGELOG 文案）→ B4 跑批/汇总 → CHANGELOG 草案（依赖 B4 数据写"验证"条目）→ 用户拍板 → 发布**；若用户要求版本号 bump 进 sha-d（serverInfo 行为面），bump 先于冻结（B4 证据 git_sha 只认 sha-d 提交）；CHANGELOG 零行为面，落后于 sha-d 不重测 |
| D4 | profile 配置纯度 | A. 全矩阵跑 full（与 sha-c 严格同构 → V1-V4 干净归因）；B. 按发布真实配置 core（混入工具面变量） | **A（2026-09-05 定案）**：E-02/E-06 路径工具（task_create/spec_get/task_list 等）全部在 core 面内（裁剪的 9 个 = agent_* 自动面 + hook_* 配置面，与 E-02/E-06 行为路径无关）——core/full 对 B4 结果等价，故全矩阵跑 full 保与 sha-c 同构；core 裁剪正确性由 mcp-profile.test（33 工具集合差）与真进程冒烟覆盖，无需 B4 真机 |

## 八、产出与后续

- B4 证据（sha-d 批契约物，validator strict 0 ERROR）提交 + **B4 对照汇总**（DeepSeek- 前缀，同目录）。
- 依 B4 数据出结论：V1（G1 修订生效 / 触发裁决 1.4 场景语义演进登记 04-00）；V2（G5 生效 / 归档语义交用户裁决）；V3（E-06b 复现稳定 / 样本噪声）；V4（L7+契约发布面确认）→ 依 B3 汇总 §四.3 走 05-00 T-006 字段级裁决落库（B3 证据已足，B4 补充 sha-d 行）。
- **CHANGELOG/版本/发布——停下与用户讨论（硬性门禁，D3 顺序）**。
