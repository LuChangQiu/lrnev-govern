# T-027 放量编排方案裁决（0fa12b9d D1-D6）

- **日期**: 2026-09-03
- **作者**: DeepSeek（审查方）
- **依据**: 0fa12b9d 放量编排方案 + 裁决文档（证据契约/接入）+ tasks T-027
- **结论**: 方案总体采纳。D1-D6 裁决如下。

## 裁决

| # | 问题 | 裁决 |
|---|---|---|
| D1 | 采样档位（A 档 288 sessions） | **采纳 A 档**（explicit 6×5×2SHA×3客户端=180 + 记录组 6×3×2SHA=108）。**但按方案分阶段执行**：先 claude-code 全量（R1-R3）验证系统与数据质量，再 codex/opencode——不一次烧预算 |
| D2 | 方差口径（依赖代理层？） | **双轨**：先行采用**可采口径**——action 级布尔方差（同场景 5 次 action_taken/成功率 std/mean<10%）+ 工具序列组成方差；surface 级方差（guidance_surfaces）待代理层（Q1-A）就绪后补充，不阻塞放量启动 |
| D3 | E-06b 完成规则 | 分轮实现 + sha-a 跑通 + 证据含两轮记录即算 E-06b 可放量；AI 首轮不建 B 等行为随机按数据记录（重试 ≤2 次）；E-06b cell 可最后跑，不阻塞其他场景 |
| D4 | wrapper env T027_SHA 优先 | **采纳**（根除指针竞态的最根本手段）。实施排在 wrapper 代理改造（fd06d361）之后——同一文件，由放量前置改造 agent 一并加：读 env `T027_SHA`（sha-a/sha-b）优先，无 env 回退指针文件；session 前后指针/HEAD 审计写 sidecar（不进 evidence 契约键） |
| D5 | 预算上限 | **采纳 $120 封顶**（名义 $30-75 + 重试余量）。每 cell 完成后报告实际成本；超 $120 停止并报告 |
| D6 | commit 粒度 | **每 cell（客户端×SHA）完成后证据+manifest 提交一次**；复审关口：R1 试点（15 sessions）后、claude 双 SHA 全量后、R-final 汇总。DeepSeek 每关口审查 |

## 方案确认要点（采纳）

- evidence 移 `.evidences/scale/<client>/<sha>/<scenario>/`（与 legacy/INVALID 隔离）✅；
- 每 session 验收：exit 0/1/4 为数据不重试；validator strict 0 ERROR、工具纯净、mcp_version=2025-11-25、git_sha 匹配期望、sha_audit 一致 → VALID；429 ≥60s 指数退避；AI 行为失败一律当数据 ✅；
- manifest：每 client×sha 一份 b2b 兼容（stage=T-027 + sha_label + 40hex；failure_class 留 DeepSeek 填）✅；
- F-04 A-E 候选统计表 + 双 SHA 归因报告结构 ✅。

## 前置依赖（执行顺序）

1. E-06b 合入（74877b63）→ 2. wrapper 代理（fd06d361）+ env T027_SHA 优先 → 3. **harness 放量版改造**（evidence 对齐裁决：64hex fixture_hash/decision_context=null+sent:false/fixture_context/client_version/mcp_version 2025-11-25/去 client/_debug 键/validator 门禁）→ 4. Codex/OpenCode 驱动分支 → 5. R1 试点 15 sessions → 复审 → 扩量。
