# T-027 R1 试点复审报告（暂停点：等用户确认扩量）

- **日期**: 2026-09-03 夜
- **作者**: DeepSeek（审查方）
- **数据**: 372bf049 执行；提交 366124c/a9036d4/311f71f/f1cccba；15/15 VALID；成本 $5.02
- **状态**: ✅ R1 完成（自主运行目标④达成——暂停等用户）

## R1 数据审查结论

| 场景 | 5 次判定 | 成功率 | 方差 | 与冒烟一致性 |
|---|---|---|---|---|
| E-01（explicit new_spec） | 5/5 PASS | 1.0 | std=0 | 一致 |
| E-02（explicit reuse_spec） | 5/5 FAIL | 0.0 | std=0 | 一致 |
| E-07（explicit no_spec） | 5/5 PASS | 1.0 | std=0 | 一致 |

- 验收：15/15 validator strict 0 ERROR、工具纯净（73/0/42）、契约字段全达标；
- E-01：spec_create 稳定（name 全对、scene 完整 id/别名混合但服务端解析正确）；
- E-07：no_spec 稳定（0 治理工具；action_success=false 为字段口径 artifact，以 session 判定为准）；
- 成本 $5.02（预算 $50 的 ~10%）；零环境重试。

## 关键发现 1：E-02 5/5 FAIL 触发 F-04 门禁（需用户决策）

**机制**（5/5 稳定）：explicit reuse 意图下，AI 全部走 `project_status → spec_get(A) → Read A 三文档 → 直接 Edit/Write 落位 A`，**从不调用 task_create(A)**；对 .lrnev 文档的 Edit 在 -p 权限下被拒循环（s4 转投 task_create_many 但非期望工具/参数）。

**定性**：A 类关键失败候选（explicit 用户目标未被正确执行——用户"在登录 Spec 里补充"的治理语义 = task_create 登记，AI 却试图直接编辑文件）。**F-04 门禁：同场景 ≥2/5 → 触发修复重测**——E-02 5/5 已触发。

**可能根因（待分析）**：① 服务端 guidance 对"在已有 spec 下登记工作应调 task_create"的引导不足（AI 从 spec_get/Read 未获得该提示）；② 场景 prompt 无额外提示（盲测纪律下 AI 依赖自身知识与服务端 instructions）；③ 模型对"编辑 spec 文档=完成用户请求"的误解。修复方向属 05-00 Profile/guidance 改进范畴或场景 fixture 微调——**需用户确认后处理**（不擅改盲测场景）。

## 关键发现 2：扩量前遗留（冒烟审查记录，等用户确认一并处理）

1. E-05/E-06a 多轮注入缺陷 → 分轮改造（E-06b 分轮后 PASS 佐证）；
2. E-09 fixture expectedArgs '00-login' → '01-00-login'（参数级误判）；
3. E-04 判定复核（文本询问符合语义但被判 FAIL）；
4. E-02 门禁处理（上述）。

## 建议的扩量路径（等用户拍板）

- R2/R3：claude-code 双 SHA 全量（12 场景 × sha-a/sha-b × 5）——**建议先处理遗留 1-3 再扩**（否则 E-05/06a/09 的数据继续无效）；
- R4/R5：codex（CODEX_HOME 隔离、429 退避已备）；R6：opencode（DEEPSEEK_API_KEY 已配置）；
- 预算：R1 后已花 ~$8（冒烟 $3.1 + R1 $5.0），$50 闸剩余充足。

## 暂停等用户确认的事项

1. 是否按建议先修遗留 1-3 + E-02 分析后再扩量（推荐）？
2. E-02 门禁处理方向（guidance 引导改进 vs 接受为真实 AI 行为数据记录）？
3. 扩量启动授权（R2/R3 claude 全量 → R4/R5 codex → R6 opencode）？
4. T-027 task_update 落库与 05-00 合入时机（全部数据到位后）。
