# 常驻提示词模板草稿（lrnev + CodeGraph 版 / 单 lrnev 版）

- 日期：2026-09-07（DeepSeek 子代理起草，供主代理与用户审定）
- 任务：按用户给的"三工具版参考结构"（`# 工具栈与工作流` → 工具速查表 → 核心规则 → 各工具使用规则 → 串联流程）起草两个可直接粘贴进 AGENTS.md/CLAUDE.md 的模板
- 范围：只新增本文档，未改其他文件、未 commit
- 措辞基准：docs/AI-ADAPTATION.md §"常驻提示词模板" + src/mcp/guidance.ts（WORKFLOW_OVERVIEW / TOOL_DESCRIPTIONS / GUIDE_SECTIONS）+ src/core/SpecGuidance.ts（E-02 G1 修订）+ src/core/guidance-semantics.ts（用户决定优先条款等）+ src/types/spec.ts（状态机）+ TaskManager 的 completed-spec 提示；校对点逐条见文末清单

---

# 模板 A（lrnev + CodeGraph 组合版）

> 适用：中大型/多包代码库、开发实现类会话（详见文末起草说明）。粘贴位置：项目根 `AGENTS.md`（通用）/ `CLAUDE.md`（Claude Code）/ Cursor 规则文件。
> 用量：粘贴区正文去空白 ≈2239 字 / 汉字 789 / 汉字+ASCII 词 940（三口径数值见文末 §四），与"2000 字内"目标的对标说明见 §四。

⬇⬇⬇ 以下为可直接粘贴内容（不含本行与末尾标记行）⬇⬇⬇

# 工具栈与工作流

双工具分工：**lrnev** 管治理——该不该做/做到哪/怎么验收/留什么记录；**CodeGraph** 管代码——在哪定义/谁调用/改动波及谁。纯查代码走 CodeGraph；要改代码或推进治理才走 lrnev 流程。

## 工具速查表

| 工具 | 职责一句话 |
|---|---|
| lrnev 接手（只读） | project_status 现状快照；governance_map scene→spec 全景；context_search 检索治理文档；spec_get 读三文档 |
| lrnev 落位推进 | spec_create 开 Spec；task_create(_many) 登记任务；task_update 推进；spec_gate_check 门禁；spec_update 状态回填 |
| lrnev 轻产物 | error_record 踩坑；adr_create 决策；memory_save 约定；assess_goal 单多 Spec 判断 |
| CodeGraph explore（主力） | 一次命中符号+来源文件+调用路径（X 在哪/谁调 X/波及谁） |

## lrnev 核心规则

1. **只读 vs 要改**：纯查代码/定位/解释/回答 → 直接做，不 project_status、不开 spec；以下流程只在要改代码或推进治理时走。
2. **要改且不确定进度** → 先 `project_status`；全景 `governance_map`；找相关文档 `context_search`。
3. **开不开 spec、开在哪，自己判断、便宜先**：
   - ① 写不出独立 WHEN…THEN 验收的小改动（改文档/注释/排版/小重构/调参…）→ 直接做；
   - ② **开发/扩展请求（加/实现/补充某能力）一律先登记再实施**：`context_search`/`spec_get` 定位承载 Spec → `task_create` 登记 → 再动手。直接编辑 requirements/design 只许需求细化/文档维护，不能替代登记；Spec 已 completed 时登记会提示：可 `spec_update` 合法回退 in-progress（维护态新增）；
   - ③ 独立可交付新特性才 `spec_create`：能写 WHEN…THEN 验收 + 可独立交付，两"是"才开。优先归已有 scene；新业务域经用户确认才 `scene_create`；无稳定域小特性才落 00-default（兜底）；scene/00 拿不准问用户；
   - ④ 整体推翻需求/设计 → 开新版（version+1，VV 是重写版号非修订号）留旧版对照；archived 是终态，归档/撤销只由用户定——AI 不自动归档，刚建的 Spec 不得自行回退；
   - ⑤ 旧 Spec 上下文冷却（久未动/已 completed/任务已清空）→ 先 `context_search` 读摘要，再决定复用（task_create 落位）、开新版还是新建；
   - ⑥ 用户点名要建 spec/scene → 尊重决定直接建（以上均可被用户要求覆盖）。
4. **踩坑→`error_record`，决策→`adr_create`，约定→`memory_save`**；不沾的直接做。
5. **多特性需求**先判断单/多 Spec（`assess_goal`：single-spec / multi-spec-program / research-program，建议可跳过）。
6. **改前** task_update(in_progress)，**完成** task_update(completed)。
7. 不清楚工具/流程或 gate 报错 → `lrnev_guide`。
8. **Gate**：`spec_gate_check`(gate=creation/ready/completion)，未过按 checks 的 hint 修，达标再 spec_update 推进。
9. `lrnev_report` 是给人看的只读体检（欠债/收口/validates），想看欠债时调，非必走 gate。
10. 结构/状态异常（broken/缺文件）→ 先 `lrnev_doctor`。

## CodeGraph 使用规则（按你环境实际暴露的工具调整）

- 只读查询、不改文件；查代码用它，治理判断与动作仍走 lrnev。
- **先查后读**：先 explore 命中符号/文件再 read，别盲扫文件树。
- **explore 是主力**：一次覆盖多符号，返回来源+调用路径+影响面。
- 默认约 4 个只读工具、命名各异：粘贴前按实际名单替换占位。
- 索引是本地派生物（不进 git、可重建）：查不到/疑似过期 → 重建或 grep/read 核源码——源码是真相。

## 串联流程

1. 只读请求：CodeGraph 定位 → read → 直接答；要回看验收口径再只读 spec_get/context_search。
2. 要改请求：按规则 2/3 定落点（便宜先）——小改直接做；已有 Spec 增量 → context_search 定位 → **task_create 登记** → 实施（codegraph 定位代码、spec_get 对照口径）；新特性 → spec_create → 填 requirements → ready gate → 拆任务（整单 task_create_many、补单 task_create）。
3. 执行收尾：任务前后 task_update；全完成 → completion gate → spec_update 回填 completed → 回看 L0 与验收；轻产物按规则 4；不懂 lrnev_guide、异常 lrnev_doctor。

⬆⬆⬆ 模板 A 粘贴到此为止 ⬆⬆⬆

---

# 模板 B（单 lrnev 版）

> 适用：中小项目或治理驱动项目，代码规模小到内置 grep/glob/read 够用（详见文末起草说明）。
> 用量：粘贴区正文去空白 ≈1630 字 / 汉字 429 / 汉字+ASCII 词 558（三口径数值见文末 §四）。

⬇⬇⬇ 以下为可直接粘贴内容（不含本行与末尾标记行）⬇⬇⬇

# 工具栈与工作流

本项目用 **lrnev** 治理：Scene/Spec/Task、gate 门禁与轻产物全走它；查代码用 grep/glob/read（lrnev 不查源码）。`agent_*`/`hook_*` 由连接层/配置期处理，不必主动选。

## 工具速查（[核心] 档为主）

- 接手：project_status 快照；governance_map 全景；context_search 检索治理文档
- 生命周期：spec_create 开 Spec；spec_gate_check 门禁；spec_update 回填（归档只按用户要求）；spec_get/spec_list/scene_list/task_list 查明细
- 执行：task_create 补单+登记增量；task_create_many 整单拆解；task_update 推进
- 轻产物与辅助：error_record/adr_create/memory_save；assess_goal；lrnev_report 体检、lrnev_doctor 诊断、lrnev_guide 手册

## 核心规则

1. 只读 vs 要改：纯查代码/解释 → 直接做；流程只在要改代码或推进治理时走。
2. 要改且不知进度 → project_status；全景 governance_map；文档 context_search。
3. 开不开 spec 便宜先：
   - ① 写不出 WHEN…THEN 独立验收的小改动 → 直接做；
   - ② 开发/扩展请求（加/实现/补充）→ 定位承载 Spec（context_search/spec_get）→ **task_create 登记再实施**；直接编辑 requirements/design 只许需求细化/文档维护；completed spec 登记后可回退 in-progress（合法转换）；
   - ③ 独立可交付才 spec_create（两"是"）：优先已有 scene；新域经用户确认才 scene_create；无稳定域落 00-default（兜底）；
   - ④ 整体推翻 → 新版 version+1 留旧版对照；archived 终态只由用户定；
   - ⑤ 旧 Spec 久未动/已 completed/任务清空（冷却）→ 先 context_search 读摘要，再定复用/新版/新建；
   - ⑥ 用户点名建 spec/scene → 直接照做。
4. 踩坑→error_record；决策→adr_create；约定→memory_save。
5. 多特性需求 assess_goal 辅助拆分（建议可跳过）。
6. 改前 task_update(in_progress)；完成 task_update(completed)。
7. 不懂调 lrnev_guide；结构异常先 lrnev_doctor。
8. Gate：spec_gate_check(gate=creation/ready/completion)，按 hint 修后 spec_update 推进；lrnev_report 只读体检非必走。

## 快速参考

- Spec：draft→ready→in-progress→completed；completed→in-progress 合法（维护新增）；archived 终态只由用户定。Task：pending→in_progress→completed；completed 终态，返工新建 task。
- Gate：ready 查必填章节与 FILL 哨兵；completion 查任务全 completed + requirements/design 无 FILL 残留；EARS（WHEN…THEN）是推荐写法非硬规则。
- 自救：AMBIGUOUS_REF → 选完整 id 重调；ready/completion 未过 → 按 checks 的 hint 修；INVALID_STATUS_TRANSITION → 按状态机走；broken/缺文件 → lrnev_doctor。
- 收尾：阶段完成 summarize_save 更新 L0/L1 摘要；会话压缩/结束前 session_commit 沉淀候选记忆。

⬆⬆⬆ 模板 B 粘贴到此为止 ⬆⬆⬆

---

# 起草说明

## 一、两个版本各适用什么项目

| | 模板 A（lrnev + CodeGraph） | 模板 B（单 lrnev） |
|---|---|---|
| 项目形态 | 中大型/多包代码库：符号级定位、调用路径、影响面分析是日常 | 中小代码库：内置 grep/glob/read 足够导航 |
| 会话重心 | 开发实现类（频繁"找到 X 再改了它"） | 治理驱动/需求闭环/复盘类，代码量小 |
| 前置条件 | 环境挂了 codegraph MCP 且项目已建索引（如 `codegraph init`） | 无额外依赖；对弱模型、工具面板拥挤的客户端更友好 |
| 代价 | AGENTS.md 需维护第二个工具面的命名（随环境漂移，粘贴前要换占位） | 大库上定位成本高，享受不到符号级查询 |

补充：若项目还挂了 Understand Anything 之类的架构图谱工具，可按"与 CodeGraph 同类：只读、先查后读、结果以源码为准"并入模板 A 的 CodeGraph 节，不必单独成版（本草稿按任务范围只出 A/B 两版）。

## 二、与 docs/AI-ADAPTATION.md 的关系（建议：并存 + 引用，暂不替换）

- AI-ADAPTATION.md 是全生命周期适配文档（接入、profile、E2E 验收框架），其"常驻提示词模板（防长对话遗忘）"节内嵌了一段旧版 10 条模板——它仍是有效的"默认单模板"。
- 本草稿 A/B 定位是**面向用户项目粘贴的升级模板**，与 docs 的关系建议三选：
  1. **并存 + 引用（推荐）**：docs 该节保留，另加一行指引指向本模板文件；用户从两版里选定一版定稿后再回填；
  2. **替换**：定稿后把 docs 该节的模板块换成选中的新版全文（消除两份 10 条规则漂移的风险）；
  3. **只引用不内嵌**：docs 该节改为链接 + 版本说明；权威措辞永远以 src/mcp/guidance.ts、src/core/SpecGuidance.ts、src/core/guidance-semantics.ts 三个源码文件为准。
- 风险提示：AI-ADAPTATION.md 是手写副本，src 的 guidance 措辞后续演进时文档会 drift；模板定稿建议带"基于 2026-09-07 的 src 措辞生成"的版本标注（见校对点 12）。

## 三、校对点清单（哪些来自权威源、哪些是相对参考/旧模板的修正、哪些是新增）

### 相对"用户参考文本 10 条"与 AI-ADAPTATION 现有模板的修正（均有源码依据）

| # | 修正点 | 依据（权威源） |
|---|---|---|
| 1 | ②"已有特性增量→task_create 落位"从**选项**改为**执行指令**："开发/扩展请求一律先 task_create 登记再实施"；明确禁止用直接编辑文档替代登记 | E-02 实测：模型 spec_get 后直接 Edit/Write spec 文档手写任务、从不 task_create（ai-discussions/结果/2026-09-03-T027-R1试点汇总-E01E02E07-sha-a.md §1.2：5/5 FAIL；SpecGuidance.ts:26 注释"三 SHA 0/5"）；修复措辞 = SPEC_INCREMENT_GUIDANCE（SpecGuidance.ts:36-41） |
| 2 | 补充 G1 修订边界："直接编辑 requirements/design 只用于需求细化或文档维护，不能替代开发任务登记" | SpecGuidance.ts:31-34（B4 措辞修订）；guidance.ts:129 concepts 同口径 |
| 3 | completed spec 上登记新增 = 维护态工作，可 spec_update 回退 in-progress，并写明"合法"（旧模板只说"可回退"） | VALID_SPEC_TRANSITIONS：completed→[archived, in-progress]（src/types/spec.ts:29）；TaskManager.ts:206-210/410-414 followup 提示口径 |
| 4 | 归档边界补强：archived 是终态、AI 不自动归档（用户改主意不构成归档依据）、刚创建的 Spec 不得自行撤销/回退——旧模板完全没写 | WORKFLOW_OVERVIEW 归档边界行（guidance.ts:9）；spec_update TOOL_DESCRIPTIONS【决策边界】（guidance.ts:32）；SPEC_CREATION_SUCCESS_FOLLOWUP（guidance-semantics.ts:47-53） |
| 5 | 判断标尺（参考文本/AI-ADAPTATION 旧模板的"三条"）落到规则 3：整体推翻→开新版留旧版对照（④）、独立可验收→新 Spec（③）、上下文冷却→先读摘要再定（⑤） | SPEC_REUSE_DECISION_RUBRIC 三条（guidance-semantics.ts:64-97）；SpecManager.ts:225 followup 同口径 |
| 6 | VV 语义："VV 是正式重写版号、不是修订号"，避免把增量误当重写 | guidance.ts:129 concepts |
| 7 | 用户决定优先条款收进规则 3-⑤（以上建议均可被用户要求覆盖） | USER_DECISION_PRIORITY_CLAUSE（guidance-semantics.ts:31-34）；spec_create 描述（guidance.ts:29） |
| 8 | gate 三档写全 creation/ready/completion | GateType（src/types/gate.ts:2）；工具参数 enum（src/mcp/tools/index.ts:464） |
| 9 | assess_goal 返回值 + "建议可跳过" | TOOL_DESCRIPTIONS assess_goal（guidance.ts:43）；GOAL_ASSESSOR_OVERRIDE_CLAUSE（guidance-semantics.ts:104-107） |
| 10 | 00-default 语义"兜底不是默认堆放处"、scene/00 拿不准问用户 | WORKFLOW_OVERVIEW 分流行（guidance.ts:8）；SpecManager.ts:225；DEFAULT_SCENE_ID='00-default'（SceneManager.ts:34） |
| 11 | 快速参考里的状态机、gate 边界、错误自救（AMBIGUOUS_REF/FILL/终态返工新开 task 等） | GUIDE_SECTIONS errors/concepts（guidance.ts:110-135）；TaskManager.ts:1323；"EARS 推荐写法非 gate 规则"（guidance.ts:127） |
| 12 | 工具速查的职责一句话均压缩自 TOOL_DESCRIPTIONS 原句；B 版只收 [核心] 档 | guidance.ts:20-63（[核心] 12 个：project_status/governance_map/scene_list/spec_create/spec_list/spec_get/spec_update/spec_gate_check/task_create/task_create_many/task_update/context_search） |

### 草稿新增、无 src 权威源可引（需用户确认的措辞）

| # | 新增点 | 说明 |
|---|---|---|
| 13 | 模板 A 的 CodeGraph 节：先查后读 / explore 主力 / 默认约 4 个只读工具（占位）/ 索引是本地派生物、可重建 / grep-read 兜底 | 刻意不写死任何具体工具名、不抄 deepseek-harness 环境细节（那是用户另一环境的配置）；"4 工具"按用户参考文本口径写成占位 |
| 14 | 两个模板开头加"粘贴位置 + 工具名前缀以实际为准"的入口说明（放在粘贴区外） | 参考结构之外的入口提示 |
| 15 | "串联流程"两版不同：A 强调"查代码用 CodeGraph、治理动作用 lrnev"双轨配合；B 退化为规则内联 + 快速参考 | 结构对齐用户参考（# 工具栈与工作流 → 速查 → 规则 → 各工具规则 → 串联流程） |
| 16 | B 版补 session_commit / summarize_save 收尾一行 | 面向长会话压缩场景的防遗忘补充（工具描述见 guidance.ts:52/44） |
| 17 | 模板正文建议带"基于 2026-09-07 src 措辞生成"版本标注（定稿时决定是否内嵌） | 防 guidance 措辞演进后的无声 drift |

## 四、长度口径与遗留

- 实测字数（本文件粘贴区正文，三口径）：模板 A 去空白 2239 / 汉字 789 / 汉字+ASCII 词 940；模板 B 去空白 1630 / 汉字 429 / 汉字+ASCII 词 558。对照基准：docs/AI-ADAPTATION.md 现有模板区段（10 条+标尺+用户优先）实测 1602 去空白 / 715 汉字——A 版含完整 CodeGraph 节与双工具速查，去空白口径比它长 637 字；若按"汉字"或"汉字+词"口径理解"2000 字内"，A/B 两版均显著达标（A 汉字口径约为 2000 的 39%）；若按含全部字符（含中文标点与工具名）的严格口径，A 版略超 2000、B 版略超 1200，可按下条继续删。
- 压缩手段（本次已做）：速查表按工具组归并（A 原 12 行→4 行）；规则删冗余修饰与重复例子；CodeGraph 节去示例式括号注释；流程与规则互相引用避免复述。若需再压：候选 ① 删 3-④ 的"刚建的 Spec 不得自行回退"（并入归档句）；② CodeGraph 节 5 条并 3 条；③ 速查表再删"职责一句话"列。
- 遗留问题：① CodeGraph 具体工具名需用户在其项目环境替换占位；② 若定稿一版，回填 docs/AI-ADAPTATION.md（替换或加指引）由主代理后续执行——本稿未动任何现有文件；③ UA（Understand Anything）未单独成版，需要时并入 A 版 CodeGraph 节（见起草说明一）。
