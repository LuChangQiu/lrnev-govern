# T-027 客户端矩阵定案 + 并行执行指令（DeepSeek 发布 2026-09-02）

- **决策人**: DeepSeek（审查方 / 全局协调）
- **用户确认**: Claude Code、Codex 可用；**OpenCode（DeepSeek V4 Flash）加入**

## 一、T-027 客户端矩阵（最终定案）

| 客户端 | 角色 | 模型 | F-04 门禁 |
|---|---|---|---|
| **Claude Code** | 主客户端 | Claude（sonnet/opus 视环境） | 主力 ≥1/5 |
| **Codex** | 第二客户端 | OpenAI（视环境） | 主力 ≥1/5 |
| **OpenCode** | 第三客户端 | **DeepSeek V4 Flash** | 补充（≥1/5 更稳） |

> 回归用户最初方案（OpenCode + DeepSeek V4 Flash）+ Claude Code 主 + Codex 补 = 三客户端矩阵。F-03 的"每主力客户端 explicit 场景 ≥5 clean sessions"按三客户端执行。

## 二、执行顺序保障（归因陷阱已确认）

**T-027 双 SHA 对照必须在 05-00（Profile/B3）合入前执行**——否则 SHA B 混入 Profile 变量，违反 D-04 单变量原则。

## 三、并行执行安排

```
主线 A：05-00 Profile 设计/实施（已发指令，DeepSeek 逐阶段复审）
并行 B：T-027 基建 + 执行（本指令）
```

### T-027 工作分解（Claude 执行）

**Phase 1：基建（立即）**
1. **三客户端 MCP 接入配置**：
   - Claude Code：`.mcp.json` 或 claude config 注册 lrnev server
   - Codex：config 注册
   - OpenCode：opencode.json 注册 + DeepSeek V4 Flash 模型配置
2. **clean session harness 脚本**（scripts/ 下新增，如 `run-t027-session.mts`）：
   - 参数：client（claude-code/codex/opencode）、scenario（E-01~E-11）、sha（A=45a86e15 / B=当前）
   - 每次独立 session（无历史上下文复用）
   - 记录：MCP 调用、structuredContent、客户端版本、模型版本
3. **双 SHA 切换脚本**：checkout 45a86e15 / checkout B → 跑 11 场景

**Phase 2：执行（环境就绪后，05-00 合入前）**
1. 每客户端 × explicit 场景（E-01/E-02/E-06a/E-06b/E-07/E-08）≥5 clean sessions
2. 盲测原则：执行者未见期望答案
3. B0'/B1' 行为快照 + 差异对比（文本变量唯一）

**Phase 3：证据**
- 产出 T-027 盲测报告 + 行为基线（引用 04 证据格式）
- 提交 DeepSeek 复审

## 四、与 05-00 的时序

```
现在：05-00 设计（A）∥ T-027 基建（B-Phase1）
05-00 实施中：T-027 双 SHA 执行（B-Phase2，05-00 合入前完成）
05-00 合入后：B3 观测（新变量，另算）
```

## 五、红线

- 02-00 基线不可变、证据禁改写
- 双 SHA 归因纯净（只有文本变量）
- 盲测执行者未见期望答案（T-027 acceptance）
- 每客户端 ≥5 clean sessions 独立
- 落库=task_update、先复审后落库

**开始**：Claude 并行推进 05-00 设计（提交复审）+ T-027 Phase 1 基建。
