# 05-00 T-004 侦察裁决（b7a58c63 六问）

- **日期**: 2026-09-03（晚，收尾）
- **作者**: DeepSeek（审查方）
- **依据**: b7a58c63 T-004 侦察蓝图 + requirements F-03/F-06/F-07 + design D-04/D-06 + 既有裁决
- **状态**: 裁决完成；T-004 实施明天派（今日已停）

## 裁决

| # | 问题 | 裁决 |
|---|---|---|
| Q1 | guidance 形状 | **顶层可选 `guidance?: LrnevGuidanceItem[]`**（LrnevToolPayload 顶层，与 ai_followup 等并列；不包装对象）。侦察已确认索引签名与顶层可选先例——无结构障碍 |
| Q2 | assess_goal renderer | **A：改投影 instructions**（assess-goal renderer 与其余 41 个渲染器对齐，投影 ai_followup.instructions 到 content）。理由：GoalAssessor 多条指令当前对文本通道不可见 = 01 文本降级信息缺失（违反 F-07"保留 01 文本降级"）；单文件小改 |
| Q3 | assertGuidancePublishable 语义 | **落为测试/CI 发布门禁，不在运行时写后抛错**（侦察推荐）。理由：运行时抛错诱导客户端重复写入，与"Profile 不创造硬约束"冲突；冲突仅省略 guidance + 诊断日志 |
| Q4 | 挂载点 | **tool-result-adapter 单点 + 四工具 allowlist + 数据驱动**（assess_goal/scene_create/spec_create/task_create；仅五角色前缀行 → buildGuidanceView 派生）——避免 4 handler 重复；失败/冲突仅省略 guidance，绝不翻 ok |
| Q5 | enforcement 显式化 | **不显式化**（延续 T-001 裁决 Q8：仅 role 无法推出时出现；当前文本前缀派生行隐含语义；等 04 证据按 F-01 门禁再开）。schema 保留可选 enforcement |
| Q6 | response_version | **不 bump**（guidance 为向后兼容可选附加字段；bump 破坏 B0~B2b 对照的 response_version 恒定性） |

## 实施要点（明天派 agent 时引用）

1. response-envelope.ts：LrnevToolPayload 加 `guidance?: LrnevGuidanceItem[]`（注释：可选、向后兼容、仅 role 化工具携带）；
2. guidance-profile.ts helpers 增 `classifyInstructions` 纯函数（识别五角色前缀行→语义输入）；
3. tool-result-adapter.ts 单点挂载（四工具 allowlist + 数据驱动；失败/冲突仅省略）；
4. output-schemas.ts：仅四工具 response schema 声明可选 guidance（数组项 = LrnevGuidanceItem 形状）；
5. renderers/assess-goal.ts：改投影 instructions（Q2-A）；
6. 42 渲染器其余零改动；content 文本字节不变（回归测试断言）；
7. 测试：unit（挂载派生/降级/门禁/未知 role/schema）+ integration（文本与 guidance 同源一致、无硬约束、不持久化哨兵）；
8. 全量测试 + typecheck 无新增。

## 纪律（明天）
- 不 bump response_version；不创建硬约束；guidance 失败不影响 03 data/content；客户端忽略 = capability 问题不增字段。
