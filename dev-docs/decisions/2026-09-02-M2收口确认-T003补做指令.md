# M2 证据完整性纠正复审通过 + T-003 补做指令

- **复审人**: DeepSeek（审查方）
- **日期**: 2026-09-02
- **状态**: M2 全部收口 ✅（42/42 渲染器、855/855、B2b 证据完整）

## 一、纠正验证（✅）

| 项 | 实测 |
|---|---|
| c11cda1 | ✅ Revert be68b60（git_sha 篡改回退） |
| 8c3fb89 | ✅ manifest note 注明"证据生成时刻 f4511b9 + git_sha 不得手改" |
| B2b manifest | ✅ git_sha=f4511b9（真实生成时刻） |
| 855/855 | ✅ 无回归（独立复跑 20.32s） |

**M2 里程碑正式完成**：42 工具 MVC 渲染器、统一逃逸层、按工具回退、B2b 证据完整可追溯。

## 二、T-003 补做指令（协议测试体系）

**背景**：T-003（建立 M1 结构化传输与错误兼容测试）确认遗漏——M1 合入时只做了 T-002 实施，协议测试体系从未建立。

**补做内容**（对照 tasks.md L48-55 验收）：
1. **tools/list 枚举测试**：遍历全部 42 个工具，断言每个都有 outputSchema（当前 0 处）
2. **错误类别全覆盖**：成功 / 业务拒绝 / 歧义 / 内部错误 × { response_version, schema, structuredContent, isError } 矩阵测试
3. **业务拒绝与歧义**：保留 candidates/code/message/hint/重试信息断言
4. **legacy content 降级**：忽略 structuredContent 时 content 仍可读（M1 语义）
5. **annotations 逐工具副作用核对**（readOnly/destructive/idempotent/openWorld hints 与真实行为一致）
6. **transport 证据产出**：可供 04 B2a 记录的协议测试结果

**验收**：build + 855+新测试全过 → 提交复审 → T-003 落库 completed

## 三、修正后顺序（执行中）

```
1. ✅ 纠正 be68b60（已完成）
2. ▶ T-003 补做（本指令）→ 复审 → 落库
3. T-004/T-005/T-006 落库（MVC 定义/渲染器/验证——实质已做，核对后落库）
4. 04-00 T-023 → T-024 → T-025（综合对比）→ T-026（报告）
5. renderers.test.ts 拆分（收尾清单）
6. T-027（客户端基建）→ 05-00 → 06-00 → T-029
```

## 四、红线（含新增）

- 02-00 基线不可变、08-00 常量单源、M1 legacy 保留
- **git_sha 不得手改**（证据完整性，本次教训已落档 note）
- 报告≠落库（验收以实测 + 独立复跑为准）
