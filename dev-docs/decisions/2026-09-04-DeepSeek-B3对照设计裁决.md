# B3 对照设计裁决（DeepSeek 审查方）

- **日期**: 2026-09-04
- **作者**: DeepSeek（审查方）
- **状态**: 设计定稿；执行待前置条件（输出契约修复复审通过 + sha-c 快照冻结）
- **关联**: 04-00 T-027（F-04 终版汇总"下一步"第 2 项）、05-00 T-006（B3 证据）、统一审查发现 #3

## 一、B3 定义（回顾）

F-04 终版汇总 §六.2：**B3 对照 = 主工作区（G1-G4 + 05-00 Profile）真机重测 E-02/E-06a/b（关键场景 ×5）验证改进**。

B3 是 04-00 观测序列（B0/B1/B2a/B2b）之后、以**真实客户端**对"修复后主工作区"的第三次基线对照：
- sha-a（45a86e）= B0 基线，pre-M1（无 structuredContent/outputSchema）
- sha-b（6383e99）= M2+收尾，冻结；暴露三类真实问题（E-02 引导缺口 / E-06a/b 归档模式 / 输出契约缺陷）
- **B3 = 主工作区修复后快照（sha-c）**：G1-G4（98828f2）+ 05-00 Profile（9d104f9/fe6ebe6/9a1dd60/51fdc52/b6d7029）+ 输出契约修复（审查 #3，待合入）+ 审查项 #1/#2/#4/#5/#8

## 二、B3 验证目标与判定假设

| # | 目标 | 判定口径（沿用既有） | 期望（假设，待证伪） |
|---|---|---|---|
| V1 | E-02 引导缺口修复（G1-G4 生效） | E-02 v2：task_create / task_create_many 服务端成功命中目标 A（scene/spec/描述）即 PASS | claude sha-a/b 均 0/5 → B3 期待显著改善；若仍 0/5，G1-G4 引导未被消费或强度不足 → 需诊断 guidance 消费证据 |
| V2 | E-06a/b 归档模式是否仍现 | E-06a 单 task_create 口径；E-06b B 存在性检查 | claude 两 SHA 6/10+5/10 自我归档 → B3 观测是否随 G1-G4/Profile 改善；若仍高频，design 规则本身需用户裁决（不自动回滚 vs AI 撤销直觉） |
| V3 | 输出契约修复消解（opencode 端） | 工具调用无 -32602 artifact | sha-b opencode 3/5 E-02 FAIL 部分归因 -32602 → B3 opencode E-02 应无契约 artifact，纯模型行为判定 |
| V4 | 05-00 Profile 可测收益采集 | 05-00 T-006 的字段级依据：client/version/model 维度 guidance 字段消费与改善 | 供 T-006 逐字段保留/回退裁决（本设计不预设结论） |

## 三、快照（sha-c）与运行矩阵

**快照**：主工作区修复后 HEAD（含输出契约修复提交）冻结为 `.claude/t027-worktrees/sha-c`（worktree checkout），wrapper 指向 sha-c（current-sha.txt 或 env T027_SHA）。冻结后不许改。

**矩阵**（真机，clean session ×5/场景，与 sha-a/b 同构）：

| 场景 | claude | opencode | 说明 |
|---|---|---|---|
| E-02 | ×5 | ×5 | claude = V1 主目标；opencode = V3 契约消解 + V1 跨客户端 |
| E-06a | ×5 | — | V2（claude 归档模式主观测） |
| E-06b | ×5 | — | V2 |

共 20 sessions（claude 15 + opencode 5）。codex 不跑（延续 sha-b 裁决：慢、边际价值低，sha-a 对齐已够）。

**预算**：$50 闸已花 $43.54（精确核算，2026-09-04 扫描 .evidences 全量 session.jsonl result 事件）；claude 15 sessions 预计 ≈$3（按 sha-a claude 均值 E-02≈$0.42/session、E-06a/b 更低的实测水平），在剩余 ≈$6.5 内；opencode 走 DEEPSEEK_API_KEY 另计。若 claude 单 session 成本异常超支，runner 预算闸兜底停止并报告。

## 四、执行前置（按序）

1. **输出契约修复复审通过并合入主工作区**（修复 agent 27a0ae79 产出 → DeepSeek 复审 → commit）——否则 B3 的 opencode 行被 artifact 污染、V3 无法验证。
2. **E-01/E-02 归一终判落定**（eeb1af80 产出复审）——B3 基线数字与 sha-b 终判在报告中引用一致。
3. sha-c worktree 冻结 + 隔离断言（沿用 codex 修复后的 env/config 注入 + 会话隔离硬闸）。
4. 跑批（复用 scripts/t027-batch-runner.mjs 或单场景 harness，严格 validator 0 ERROR 门禁）。

## 五、产出与后续

- B3 证据（.evidences sha-c 批）提交 + 汇总报告（DeepSeek- 前缀，放 ai-discussions/结果/）
- 依 B3 证据执行 **05-00 T-006**：逐可选字段保留/调整/回退裁决 + 适配结论（任务落库走 CLI task_update）
- E-06a/b 归档模式若持续 → design 规则裁决交用户（不在本设计内预设）
- 全部完成后进入发布收尾——**CHANGELOG/版本/发布前必须停下与用户讨论**（用户硬性门禁）
