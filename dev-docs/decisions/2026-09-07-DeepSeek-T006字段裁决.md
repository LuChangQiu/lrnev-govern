# 05-00 T-006 裁决：Profile 字段保留/回退定案（DeepSeek 审查方）

- **日期**: 2026-09-07
- **作者**: DeepSeek（审查方）
- **证据基础**: ai-discussions/结果/2026-09-05-DeepSeek-T006字段裁决证据整理.md（f7569701）+ B3/B4 对照汇总（G1 送达实证、G5 行为效果、decision_context 4 次真实命中、guidance 数组零消费）+ E-06ab 分化 Session 分析
- **状态**: 定案；实施派发；T-006 落库随实施完成

## 一、输出侧（LrnevGuidanceItem / guidance 挂载）

| 字段/面 | 裁决 | 依据（04 evidence 引用） |
|---|---|---|
| O1 text/role 化文本行（ai_followup.instructions + content） | **保留** | 唯一被消费通道：G1【建议】sha-c/sha-d 5/5 送达实证（h5h5rdkp 等）；G5 归档边界行为效果 4/5→0/5（B4 V2）走的就是 WORKFLOW_OVERVIEW + spec_update 描述的文本 |
| O2 role 枚举/ROLE_PREFIX | **保留** | 五角色前缀是文本行的组成部分（classifyInstructions 消费），测试族锁定（guidance-profile/guidance-semantics 等） |
| O3 profile_version 常量 | **保留** | 语义锚，成本近零；schema 声明保留 |
| O4 source_ref | **回退（维持 off）** | 全程门禁 off，0 序列化证据（380 录制件 + 20 次 spec_create 挂载无一条含 source_ref）——记录结论：无客户端启用证据，不扩张 |
| O5 enforcement | **回退（不序列化）** | 0 序列化证据；role 推导已足够（DECISION_BOUNDARY/EXECUTION_CONSTRAINT 由前缀表达），显式化会误导为硬约束 |
| **O6 payload.guidance 运行时挂载** | **回退挂载（保留纯函数与契约测试面）** | 三客户端实测**零消费**（380 录制件无任何引用 profile_version/role/guidance 数组）+ 每次挂载 +583 字符 ≈ 响应体积 24.2% 纯重复税（sha-c 10 次挂载实测）；B4 确认消费通道 = 文本（G5 效果不依赖 guidance 数组）。**保留**：classifyInstructions/buildGuidanceView/diagnoseGuidance 纯函数 + guidance-profile 语义对象类型与单测（未来客户端可用）；**移除**：tool-result-adapter 运行时挂载点 + outputSchema 的 guidance 字段声明（避免空字段误导与体积税）。兼容安全：guidance 从未被消费 → 移除零客户端影响 |

## 二、输入侧（decision_context / DecisionContextInput）

| 字段/面 | 裁决 | 依据 |
|---|---|---|
| I1 source=client_asserted | **保留** | 校验链真实生效：3clcp016 首次缺 direction 被 INVALID_INPUT 拒后自纠成功（唯一负向校验真实链路实测） |
| I2 strength | **保留** | explicit/new_spec 完整合法传递实证（kvlyh6zd）；规则枚举与条件必填有真实覆盖 |
| I3 summary | **保留必填** | 服务端不解析内容（裁决 Q：不解析 summary）但作为 client_asserted 上下文完整性的组成；降级 optional 削弱上下文契约——无实测收益支撑降级 |
| I4 direction | **保留** | 校验链真实生效（3clcp016）；mismatch→DECISION_BOUNDARY 行为面暂无真实客户端覆盖（0 命中）——保留字段，行为效果列入观测（B5/交接语境场景可触发） |
| I5 target_ref | **保留** | lez3l2wc scene-only 短 target_ref 被宽容解析接受（真实 1 次调用） |
| **I6 reported_user_quote** | **回退（schema 移除）** | 380 录制件 0 命中 + 设计零使用（服务端不解析 quote 内容）；移除需同步 decision-context-schema/类型/测试与 05-00 spec 文本（F-04/D-03 相关条目） |
| I7 缺失≠unspecified | **保留** | 语义正确性（T-002 验收）；无真实客户端 negative 覆盖但属协议正确性非行为改进——保留 |

## 三、观测口径修正

| 项 | 裁决 | 依据 |
|---|---|---|
| M1 evidence decision_context_sent 记录 | **修正（harness 真扫描）** | sha-c 4 次真实命中但 evidence 记 sent=false——harness 硬编码"claude -p 不传"假设（L222/L300/L317）与事实矛盾；改 v1 四工具（scene_create/spec_create/task_create/assess_goal）input 真扫描记录 |

## 四、实施清单（派发，审查后合入；3.0.0 发布内容）

1. **O6**：tool-result-adapter.ts 移除 maybeAttachGuidance 挂载点（保留函数定义或移至纯函数模块）；output-schemas.ts 移除 createGuidanceToolOutputSchema 的 guidance 字段（工厂是否保留视引用）；同步 guidance-profile-mounting/guidance-mounting/spec-create 相关测试断言（从"guidance 挂载"改"文本同源/无 guidance 字段"）。
2. **I6**：decision-context-schema.ts 移除 reported_user_quote（+ 输入 schema 类型）+ 相关测试；05-00 requirements/design 文本同步（引用字段处加回退注记）。
3. **M1**：harness-mvp.mjs decision_context_sent 改真扫描（v1 四工具 input 是否含 decision_context）。
4. T-006 task_update completed（CLI 两跳，reason 引用本裁决）。

## 五、边界声明

- G5 归档边界语义（DECISION_BOUNDARY 文本）**不受 O6 影响**（走文本通道）——B4 V2 效果保留。
- 05-00 Profile v1 的 guidance 结构化传输面回退为"纯函数库 + 契约类型"——schema 不再对 MCP 响应声明 guidance 字段；若未来客户端表达结构化需求，再按 client 驱动恢复（05-00 验收"停止增加字段"原则）。
- B4 后新增观测（E-06 征询停顿 FAIL 面、E-02 交接语境演进）分别列 04-00 后续任务与 L6 范围。
