# 05-00 T-001/T-002/T-003 侦察裁决（DeepSeek）

- **日期**: 2026-09-03
- **作者**: DeepSeek（审查方）
- **依据**: 527c8fbe（T-001 侦察）+ 2a7c4765（T-002/T-003 侦察）蓝图 + 05-00 requirements/design 权威
- **状态**: 裁决完成，可据此派实施 agent

## T-001 裁决（八问）

| # | 问题 | 裁决 |
|---|---|---|
| Q1 | guidance 挂载是否现在改信封 | **否，留 T-004**。T-001 只立契约（常量/类型/纯函数/测试），不动 LrnevToolPayload/outputSchema/渲染器/adapter/Manager（侦察红线保持）。信封挂载（Profile 数据随 03 信封交付）属 T-004 兼容降级范畴 |
| Q2 | guidance 是否仅 role 化工具携带 | **是**。T-001 的 buildGuidanceView 只供产出 role 化 guidance 的工具调用；挂载时（T-004）按工具决定携带。避免全工具 schema 膨胀（最小性） |
| Q3 | MVC 文本与 Profile 合流边界 | T-001 只立**唯一构建源**（buildGuidanceView 同时派生 textLines + profileItems）；渲染器零改动（继续投影 instructions 文本）。渲染器改从 profileItems 派生文本留 T-004 后 |
| Q4 | role 序列化大小写 | 输出统一 **UPPER_SNAKE**（FACT/RECOMMENDATION/DECISION_BOUNDARY/EXECUTION_CONSTRAINT/ACTION_HINT——与 01 文档及 guidance-semantics 常量一致）；normalizeRole 解析大小写不敏感 |
| Q5 | source_ref 格式收紧 | **T-001 不收紧**（非空字符串即可）。格式约束（工作区定位/错误码）须等 04 证据（F-01 逐字段门禁），不得预加 |
| Q6 | 版本独立断言层级 | 单元测试断言 GUIDANCE_PROFILE_ID='lrnev.guidance'、VERSION='v1' **不等于** MCP protocolVersion/response_version 常量（独立性显式断言） |
| Q7 | 04 证据门禁默认关闭 | **是**。source_ref/enforcement 门禁默认 off（不因"可能有用"加字段）；代码注释与文档写明每可选字段门禁状态（off + 开启条件=04 失败证据） |
| Q8 | enforcement none 省略 | **省略**。enforcement 仅在 role 无法推出时出现（F-03）：RECOMMENDATION 隐含 client_boundary；出现时值域 client_boundary\|server_enforced；none 不序列化 |

**附加**：guidance-semantics.ts 头注释仍列 USER_DECISION 为服务端角色——T-001 常量增补时顺手修正注释（服务端可输出角色 = 五角色，USER_DECISION 仅 client_asserted 来源，见 05-00 requirements）。

## T-002/T-003 裁决（七问）

| # | 问题 | 裁决 |
|---|---|---|
| Q1 | DECISION_BOUNDARY 通道 | **先走文本**（ai_followup.instructions 追加【决策边界】行——与现有【事实】【建议】一致）；T-001 定稿 + T-004 挂载时再映射结构化。T-003 不等待 T-001 |
| Q2 | 错误表面 A/B | **A（结构化信封）**为主：handler 内完整 schema（含 superRefine 条件规则）safeParse → 失败抛 LrnevError(INVALID_INPUT, field='decision_context[.direction]' 等, hint)——错误走 canonical errors 数组（M2 后错误契约红线）。**inputSchema 广告层带基本枚举校验**（strength/direction 枚举、source literal client_asserted）让 SDK 拦类型级错误（-32602 显式报错可接受）；**条件规则**（explicit/preferred 需 direction、unspecified 禁 direction）在 handler 层结构化抛错。不做 B（纯文本拦截绕开信封） |
| Q3 | spec_update 误传 | **接受 SDK strip 剥离、不报错**（F-04"预期不传"的实现=不声明字段+天然剥离）；负向测试固化：误传被剥离、无副作用、不落盘 |
| Q4 | target_ref 口径 | 只与**本次调用**比对（解析失败仅【决策边界】提示，不阻断、不跨调用记忆）——D-04 单次对齐 |
| Q5 | assess_goal IO 校验 | **不做**。评估非写入；真实存在性由写入工具执行时校验（真实 Constraint 先行，F-06） |
| Q6 | 命名 | **DecisionContextInput**（src/types/decision-context.ts 类型 + src/mcp/types/decision-context-schema.ts zod）——避开 evidence-contract.ts 同名 DecisionContext（04 观测形状） |
| Q7 | USER_DECISION 注释 | 归 T-001 顺手修（同上附加） |

## 实施顺序建议

1. **T-001**（常量增补 + guidance-profile 类型/纯函数 + 单元测试）——独立无依赖；
2. **T-002**（DecisionContextInput schema + 校验 + 负向测试）——依赖 T-001 常量（无强耦合可并行）；
3. **T-003**（四工具接线 + 对齐纯函数 + 协议测试）——依赖 T-002；
4. 复审关口：T-001/T-002 完成后停等 DeepSeek 复审；T-003~T-005 完成后停等。

**纪律**：改 src/ 主工作区代码可提交（05-00 分支语义），但 **tasks.md 落库（T-001~T-005 completed）须等 05-00 合入审批**（T-027 双 SHA 之后）；测试不空转；不得把 decision_context 持久化（哨兵测试：非法/合法输入均不落 .lrnev）。
