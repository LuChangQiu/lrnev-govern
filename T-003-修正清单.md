# T-003 MCP 协议契约测试修正清单

## 测试 Bug 修复（已完成）

### 1. 工具数量断言过滤错误
**问题**：测试过滤 `lrnev_` 前缀后只剩 9 个工具，但断言期望 42 个  
**修复**：去掉过滤，直接断言 `toolsList.tools.length === 42`  
**位置**：第 57-63 行，第 300-322 行

### 2. Annotations 字段名错误
**问题**：测试访问 `readOnly/idempotent/destructive/openWorld`，但实现使用 `readOnlyHint/idempotentHint/destructiveHint/openWorldHint`  
**修复**：更新测试使用正确的 Hint 后缀字段名  
**位置**：第 184-298 行

### 3. 内部错误测试期望错误
**问题**：测试期望参数校验抛出异常，但 MCP SDK 实际返回 `isError: true` 响应  
**修复**：改为验证 `result.isError === true`  
**位置**：第 163-172 行

### 4. 业务拒绝场景测试不完整
**问题**：原测试访问不存在的 spec，但这只能验证 NOT_FOUND 错误，无法验证状态机拒绝  
**修复**：改为先创建 spec 并归档，再尝试非法状态转换（archived → in-progress）  
**位置**：第 103-135 行

---

## 真实问题清单（需修实现）

### 1. outputSchema 缺失（19/42 工具）

**严重程度**：高  
**影响范围**：MCP 客户端无法获取工具返回值的结构化 schema  

**缺失工具列表**：
1. adr_create
2. adr_list
3. adr_get
4. assess_goal
5. summarize_save
6. context_search
7. error_record
8. error_search
9. error_promote
10. memory_save
11. memory_search
12. memory_forget
13. session_commit
14. lrnev_doctor
15. lrnev_hook_list
16. lrnev_hook_trigger
17. lrnev_hook_tail_log
18. lrnev_hook_enable
19. lrnev_hook_disable

**修复方案**：
在 `src/mcp/tools/index.ts` 中为每个工具注册添加 `outputSchema: createToolOutputSchema(相应Schema)`

**已有 outputSchema 的工具**（23/42，供参考）：
- lrnev_init, lrnev_guide, project_status, governance_map, lrnev_report
- scene_create, scene_list, scene_get
- spec_create, spec_list, spec_get, spec_update
- spec_gate_check
- task_create, task_create_many, task_update, task_claim, task_release, task_list
- agent_register, agent_heartbeat, agent_list, agent_unregister

---

### 2. SpecDataSchema 额外属性导致 MCP 协议错误

**严重程度**：高  
**影响范围**：spec_update 和包含 spec 数据的响应会触发 MCP -32602 错误  

**错误信息**：
```
MCP error -32602: Structured content does not match the tool's output schema: 
data/data must NOT have additional properties
```

**触发场景**：
- `spec_update` 返回 spec 数据时
- `spec_create` 返回包含额外字段的 spec 数据时

**可能原因**：
1. `SpecDataSchema` 定义不完整，缺少某些运行时字段
2. 渲染器添加了 schema 未声明的额外字段
3. `ai_followup` 或其他元数据字段未在 schema 中声明

**修复方案**：
1. 检查 `src/mcp/types/output-schemas.ts` 中的 `SpecDataSchema` 定义
2. 对比实际返回的 spec 数据，找出额外属性
3. 要么在 schema 中添加缺失字段，要么在返回前过滤掉额外属性

---

### 3. 歧义引用 AMBIGUOUS_REF 未实现

**严重程度**：中  
**影响范围**：当 spec 引用歧义时无法提供候选列表  

**当前行为**：
- 测试创建同名不同版本的 spec（如 login v0 和 v1）
- 尝试用模糊引用访问时触发 MCP -32602 错误（与问题 2 相同）

**预期行为**：
- 返回 `error.code === 'AMBIGUOUS_REF'`
- 提供 `error.candidates` 列表供用户选择

**修复方案**：
1. 先修复问题 2（outputSchema 匹配）
2. 检查 `SpecManager.get()` 或 `getSpecWithGuidance()` 是否实现了歧义检测
3. 如未实现，需在这些函数中添加歧义检测逻辑并返回 `AMBIGUOUS_REF` 错误

---

## 测试执行结果

**修复前**：8/14 失败  
**修复后**：待验证（预计仍有 1-2 个失败，需修实现）

**通过的测试**（6/14）：
- ✓ 42 工具数量正确
- ✓ 成功响应包含 structuredContent
- ✓ legacy 降级 content 可读
- ✓ 写操作未误标 readOnly
- ✓ transport 证据产出正确

**失败的测试**（8/14 → 预计修复后 1-2/14）：
- ✗ outputSchema 缺失（19 个工具）→ **需修实现**
- ✗ 业务拒绝场景 → **已修测试，待重测**
- ✗ 歧义引用 → **需修实现**
- ✗ 内部错误验证 → **已修测试，应通过**
- ✗ readOnly annotations → **已修测试，应通过**
- ✗ idempotent annotations → **已修测试，应通过**
- ✗ destructive annotations → **已修测试，应通过**
- ✗ openWorld annotations → **已修测试，应通过**

---

## 后续行动

1. **立即**：提交测试修复（本次任务范围）
2. **P0**：修复 outputSchema 缺失（19 个工具）
3. **P0**：修复 SpecDataSchema 额外属性问题
4. **P1**：实现 AMBIGUOUS_REF 歧义处理
5. **P2**：验证所有 annotations 语义正确性（当前仅验证字段存在）

---

## 验证证据

**测试文件**：`tests/integration/mcp-protocol-contract.test.ts`  
**实测工具数**：42（9 个 lrnev_ 前缀 + 33 个无前缀）  
**修复行数**：~200 行  
**commit 信息**：待提交
